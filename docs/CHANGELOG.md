- 2026-08-19 [convention] /program/day restyled to cream/orange design system (behavior unchanged)

- 2026-08-19 [convention] Program /dashboard rebuilt as collapsible module view on cream/orange design system; /program (app) shell is light; Curriculum nav hidden (route kept); heatmap kept for talent

- 2026-08-20 [schema|rule|convention] Interview Agent Phase 2+3 (plan 072): InterviewTurn + InterviewReport tables folded into the still-unapplied 20260813000000 migration (NOT applied anywhere — no non-production DB exists). Scoring is now fully deterministic and model-free: scoreQuestion (clearing minEvidence = 70, full checklist = 100, +5 per cleared escalation rung, capped) -> scoreModules over the 8-module curriculum (unassessed modules report null, never 0) -> assessCompetencies against the existing RUBRIC weights -> overallFromCompetencies. judgeInterview/aggregateScores are off the live path (evaluation.ts kept as reference). Only CORE questions score; EXTENSION answers are reported under beyondMilestone and excluded. The LLM writes prose ONLY: strengths/improvements/summary/recommendation, each requiring evidenceRefs, and any item citing an unanswered question is dropped in code (filterNarrative). suggestedDays come from the cited question's own sourceDays, never the model. Report validated by interviewReportSchema on write AND on read. Report generation is provider-injected (AskJson) so it runs without server-only; report-provider.ts binds Claude. COMMUNICATION tier now derives from matched-evidence + relevance rather than the conceptualFound keyword flag. Voice (Phase 3) is turn-based, NOT Realtime: /api/interview/stt (multipart -> gpt-4o-mini-transcribe) and /api/interview/tts (takes an interviewId and NO text — the line is read from the server's own transcript so the endpoint cannot be used as free TTS), behind ENABLE_INTERVIEW_VOICE, with the legacy safetyIdentifier sha256(memberId) ported. Voice reaches the interview through the same submitInterviewAnswerAction as the text runner, and the typed fallback is always available.
- 2026-08-20 [rule|convention] Interview Agent Phase 1 (plan 072): adaptive depth ladder on the AI Cohort milestone interview. CORE bank questions stay byte-identical per blueprint (comparability); adaptivity is (a) ESCALATE onto banked deepProbes when an answer clears its minEvidence, capped at 2 and gated by a consecutive-strong/weak competenceSignal, (b) banked scaffoldProbes when below the bar, (c) up to 2 EXTENSION questions from cohort days PASSED beyond the blueprint scope, excluded from the comparable score. Questions are grounded by a templated factual clause from the member's own ProgramMissionSubmission/ProgramProject rows (never generated; omitted when the artifact is absent). Off-topic is now semantic: `relevance` ON_TOPIC/PARTIAL/OFF_TOPIC from the model, and the keyword OFF_TOPIC_PATTERNS list was DELETED from heuristics.ts (degraded turns never redirect). Deep-probe answers are graded against the rung's own expectedEvidence and filed under `qid@L{n}`. Also fixed a broken merge artifact in schema.prisma (GeneralInterviewStatus was missing its closing brace, so prisma generate failed).
- 2026-08-20 [rule] Campus Ambassador onboarding is off-site (abtalksca.netlify.app); dashboard apply stopped; challenge-enrolled students get banner + derived bell notice
- 2026-08-20 [convention] Reverted PR #153 chatbot-knowledge-base from master (old modernist UI and extra KB ingest that landed with it)
- 2026-08-20 [rule] US AI cohort (name "AI Cohort USA") stays unfrozen until every ENROLLED/COMPLETED member has passed Day 31; India still freezes on endsAt
- 2026-08-18 [rule] ViCoDathon placement certs issuable on production via --all --allow-production on issue-hackathon-award-certificates.ts
- 2026-08-18 [rule] ViCoDathon placement certs (winner/2nd/3rd/top5) as extra HACKATHON rows with metadata.hackathonVariant; same overlay layout
- 2026-08-18 [rule] Admin community synergy grant cap raised from 2000 to 3000

- 2026-08-18 [rule] Admin reset/reject clamps User and StudentProfile synergy at 0 and writes BALANCE_RECONCILIATION for already-spent submission points so the ledger cannot go negative

- 2026-08-18 [schema|rule|convention] Synergy became one User-level wallet for challenge and hackathon students (with a temporary StudentProfile rollback mirror and ledger reconciliation); all Neon mutations must target a production child branch unless that exact production write is explicitly authorized

- 2026-08-18 [schema] Reverted PR #168 AI cohort interview foundation from master (code + unused 20260813000000 migration file; do not apply/drop that schema on production)

- 2026-08-18 [convention] New UI follows docs/design-system.md (plan 071 orange/cream); modernist/pre-modernist retired as templates, existing screens unchanged

- 2026-08-18 [schema|convention] Plan 067 notification bell: Notification + NotificationRead models (read state keyed by opaque string, NOT an FK) and /admin/notifications composer; automated workshop/hackathon/cohort notifications are DERIVED at read time from EVENTS, HACKATHON config and ENROLLING ProgramCohort rows — no rows, no cron, read path stays write-free. NotificationProvider sits above SynergyProvider in the root layout (BottomNavGate needs it) and fetches only when a bell trigger mounts; one NotificationBellButton renders desktop-only in AppHeader and mobile-only as a 6th item in the bottom pill (deliberately outside the `tabs` array so the sliding indicator still measures correctly). Event notifications are suppressed for users they no longer apply to: an existing WorkshopRegistration for that eventId, an existing HackathonParticipant row (which also inverts the hackathon set — "register now" for non-participants, kickoff/deadline for participants only), or an existing ProgramMember row for that cohort. The bell shows only the newest 5 items (FEED_LIMIT in get-notifications.ts) — older ones fall off on their own, so there is deliberately no dismiss/remove control and no dismissed state in the schema

- 2026-08-17 [schema] College catalog table (54,651 institutions) + StudentProfile.collegeId (nullable, no FK) so registration/profile can store a canonical pick while college stays the display string

- 2026-08-12 [convention] App fonts self-hosted via next/font/local + src/fonts (no next/font/google fetch at build)
- 2026-08-12 [convention] Open-right-now program cards each mount GhostCursor tinted to that card’s brand color (`three` postprocessing)
- 2026-08-13 [rule] Hackathon ViCoDathon 2026 participation certificates (ABT-HK-XXXXX); HACKATHON_CERTIFICATE_TEMPLATE_URL/PATH per-type template
- 2026-08-12 [rule] Program behind-by / at-risk pace uses cohort calendar day, not Day-4 unlock ceiling (calendar+3), so on-time members are not falsely flagged ~4 days behind
- 2026-08-12 [rule|convention] Program day boundaries: PROGRAM_TZ America/Chicago → Asia/Kolkata; admin cohort startsAt/endsAt round-trip via fromZonedTime(PROGRAM_TZ) so Mission Control cohort day matches today's mission
- 2026-08-11 [convention] `/` logged-out landing is Figma rounded hub (`LandingHub`), not ModernistLanding
- 2026-08-11 [env|convention] Site help chatbot behind ENABLE_CHATBOT: /api/chat + ChatWidget in root layout, knowledge/ KB, optional GEMINI_API_KEY / ANTHROPIC_API_KEY for generation
- 2026-08-11 [convention] Privacy Policy polish (v2026-08-11): plain-English section leads, DNT, hosting logs/IP, DPDP lawful-basis framing, concrete security measures, material-change notice (email/banner/re-accept); PRIVACY_VERSION bump triggers reconsent banner
- 2026-08-10 [convention] Modernist app retheme — HSL token palette, --radius 0, Archivo, light-only foundation (plan 058)
- 2026-08-10 [schema|convention] Plan 061: cookie chooser is a small bottom-right banner (no overlay); entity details published (ABTalksOnAI / Suman Shukla / Udyam UDYAM-UP-29-0250625 / Ghaziabad address — no PAN/bank/IFSC); NewsletterSubscription model + pre-checked newsletter opt-in on all signup funnels (excluded from submit gate); Privacy/Terms wording updated to match. Migration 20260810180000_newsletter_subscription applied to Neon on 2026-08-11 (additive only: CREATE TABLE + indexes + FK)
- 2026-08-10 [rule] Consent now recorded at OAuth signup: auth.ts events.createUser writes TERMS+PRIVACY rows under source "oauth_signup" when the adapter first creates a User, closing the gap where Google sign-in created an account before any consent form was reached. /login carries a Terms/Privacy/18+ notice (notice, not checkbox — returning users have already accepted). Wrapped in try/catch; auth.config.ts untouched to stay edge-safe
- 2026-08-10 [rule|convention] Plan 060 legal hardening: entity + Grievance Officer blocks (24h ack / 15d resolve) on /terms /privacy /contact with <<FILL>> placeholders; draft banner removed; DPDP s13/s14 rights; certificate non-accreditation, Synergy Points non-currency, Fees, Indemnity, 30-day notice-before-suit; new /cookies + /contact; blocking cookie consent modal (all|limited|essential) gating abtalks_ref/abtalks_src in middleware, stored in abtalks_consent cookie with no DB row; DSAR email notification + /admin/data-requests; reconsent banner on version bump. Client-safe constants split into src/lib/legal-constants.ts (importing @/lib/legal from a client component pulled node:fs/promises into the browser bundle). Versions bumped to 2026-08-10. Migration 20260808120000_legal_consent_and_rights applied to Neon on 2026-08-11 — LegalConsent and DataRightsRequest now exist, so consent recording, the reconsent banner and /admin/data-requests are live
- 2026-08-08 [schema|convention] Added LegalConsent + DataRightsRequest tables, ProgramMember.recruiterVisibilityConsentAt, public /terms /privacy + funnel consent logging (DPDP-oriented)
- 2026-08-06 [schema] Baselined orphaned HackathonProblem/HackathonSubmission migration and added /hackathon/submission flow on the existing tables to avoid shared-Neon drift
- 2026-08-06 [rule] Hackathon registration stays open until registrationClosesUtc (Fri 7 Aug 2026 6:00 PM IST); registrationOpen remains an emergency kill switch
- 2026-08-07 [rule] Hackathon registrationOpen kill switch set false; unregistered /hackathon/dashboard visitors see closed message instead of register redirect
- 2026-08-09 [convention] /admin/students lists challenge + hackathon via track filter (ALL|CHALLENGE|HACKATHON)
- 2026-08-09 [rule] Adjusted hackathon submission deadline to Sun 9 Aug 8:45 PM IST
- 2026-08-10 [convention] /admin/submissions gains Hackathon sub-tab for HackathonSubmission feed + CSV
- 2026-08-10 — `/` now renders the landing hub for signed-in users too (no more redirect to /dashboard); track cards show "Open dashboard" per-track via `features/landing/get-landing-state.ts`; `/login` bounces signed-in users to `/` instead of `/dashboard`.

## Pending reconcile
- 2026-09-26 [convention] Landing Google tag is immediately followed by the Ads program-registration conversion snippet (AW-18456978326/NG4TCOnlroMdEJbH_OBE) plus gtag_report_conversion, so that event also fires on logged-out `/` page load
- 2026-09-25 [convention] Google Ads AW-18456978326 config on the existing gtag when ad consent is granted; conversion fires only after Snowflake or Databricks AI enrolment success
- 2026-09-25 [rule] Cohort landings /program/databricks, /program/snowflake, and /program/databricks-ai are public exact matches for marketing; day routes stay session-gated
- 2026-09-25 [env] /hire Gemini brief parsing reads `GEMINI_API_KEY` (shared with résumé/interview/chatbot); `GEMINI_RECRUITER_SEARCH` removed
- 2026-09-25 [convention] /hire brief parsing is Gemini-first: `features/hire/gemini-brief.ts` (server-only; own key `GEMINI_RECRUITER_SEARCH`, never `GEMINI_API_KEY`; model `HIRE_BRIEF_GEMINI_MODEL`) feeds the composer ticks via POST `/api/hire/brief` (SEARCH bucket, `brief:` subject) and `runScoutTurn` merges the same grounded patch (`features/hire/hire-brief.ts`) into the turn spec; `spoken-brief.ts` regex is the fallback; remote/hybrid map to `workMode` only, never `locationCity`
- 2026-09-25 [convention] `features/hire/candidate-summary.ts` is the single source of recruiter-facing evidence wording (mission line, verified-evidence emptiness, card one-liner vs View Details summary); every Hire search surface builds copy through it at render, and stored `TalentRequestMatch.rationale` is sanitised on render so pre-existing rows cannot print `AB-####`, `Gaps:` or em dashes; `AB-####` (`refPublicId`) is no longer displayed on any search or detail surface, only used as the /hire/evidence cache key
- 2026-09-23 [convention] project-context, CLAUDE.md, ARCHITECTURE (already current), and recruiter-search-qa reconciled to 078 complete; original operational tables described as dropped; dual-write / ENABLE_NEW_* retired; CHANGELOG pending list not cleared
- 2026-09-23 [schema] 078 closed: production dropped eight original legacy tables plus User.synergyPoints after model-free app deploy; archives retained; migration flags/scripts retired
- 2026-09-23 [schema] 078 final cutover: removed StudentProfile/Enrollment/ProgramMember/Certificate/Submission/QuizAttempt/ProgramMissionSubmission/SynergyEvent Prisma models and User.synergyPoints; deleted dual-write.ts; archives retained; production tables not dropped until matching app deploys
- 2026-09-23 [schema] 078 final drop rehearsal on child plan-078-final-drop-rehearsal-20260922T193545Z (br-blue-bread-amu86oi1 / ep-dark-bar-amxz0fjv): original ProgramMember/Enrollment/Submission/QuizAttempt/PMS/Certificate/SynergyEvent/StudentProfile tables and User.synergyPoints dropped; archives unchanged; production not dropped
- 2026-09-23 [schema] 078 final: ProgramMember inbound Prisma relations removed to match DB (children use programEnrollmentId); remaining current-state Enrollment/StudentProfile readers switched to ProgramEnrollment/CandidateProfile
- 2026-09-22 [schema] 078 final Gate 2/3 writers: new challenge enroll and AI-cohort membership mint ProgramEnrollment only; ProgramMember anchors stopped; historical readers use archive models
- 2026-09-22 [schema] 078 final Gate 1: immutable HistoricalQuizAttempt / HistoricalProgramMission / HistoricalCertificate / HistoricalSubmission / HistoricalSynergyEvent / HistoricalStudentProfile archives; unique history copied raw; source tables retained
- 2026-09-22 [schema] 078 final Wave 3: ProgramEnrollment.joinedAt backfilled from Enrollment.createdAt / ProgramMember.createdAt; first-track order no longer uses live Enrollment.createdAt
- 2026-09-22 [schema] 078 final Wave 2c: dropped ProgramMember inbound FKs; memberId retained as scalar; ProgramMember table kept for remaining runtime identity reads
- 2026-09-22 [schema] 078 final Wave 2b: ProgramMember child tables now FK to ProgramEnrollment.programEnrollmentId (pe_pm_*); RecruiterShortlistItem.candidateUserId FK to User; memberId columns retained
- 2026-09-22 [schema] 078 final Wave 2 additive: ProgramMember child rows gained programEnrollmentId (pe_pm_*) and RecruiterShortlistItem.candidateUserId; production backfill missing=0; memberId FKs retained until drop gate
- 2026-09-22 [convention|env] 078 final Wave 1: removed runtime migration flags (ENABLE_DUAL_WRITE, ENABLE_NEW_*, ENABLE_LEGACY_*_MIRROR); canonical family paths are unconditional; mirrors are no-ops; no tables dropped
- 2026-09-22 [convention] Phase 8-D COMPLETE: retired global dual-write runtime gating and unsafe legacy-authority rollback writers; canonical family writers are unconditional; ENABLE_DUAL_WRITE remains false; historical/structural/compliance legacy support retained; no tables dropped; Phase 8-E not started
- 2026-09-22 [env] Phase 8-C COMPLETE: production ENABLE_DUAL_WRITE=false PHASE8C_DUAL_WRITE_OFF_TIME=2026-09-22T16:35:04.000Z; global dual-write runtime shut down; dedicated family flags unchanged; runDualWrite remains; no tables dropped; Phase 8-D not started
- 2026-09-22 [convention] Phase 8-B: writeClient always uses the Neon direct session (independent of ENABLE_DUAL_WRITE); challenge pe_enr_* writes via applyChallengeProgramEnrollment outside runDualWrite; production ENABLE_DUAL_WRITE stays true; Phase 8-C not started
- 2026-09-22 [env] W8-B COMPLETE: production ENABLE_LEGACY_PROGRAM_MEMBER_MIRROR=false W8B_CUTOVER_TIME=2026-09-22T05:55:38.000Z; ProgramEnrollment pe_pm_ is sole ProgramMember-domain current-state+write authority; ProgramMember mutable state frozen (Outcome A structural anchors remain for FKs; anonymize still scrubs PII); dual-write stays; ProgramMember not dropped
- 2026-09-22 [env|convention] W8-B ProgramMember compatibility freeze: ENABLE_LEGACY_PROGRAM_MEMBER_MIRROR=false freezes ProgramMember mutable current-state; ProgramEnrollment pe_pm_ remains sole membership/score/unlock/recommendation authority; Outcome A keeps minimal ProgramMember structural anchors for FKs; anonymize still scrubs frozen PM PII; dual-write stays; ProgramMember table is not dropped
- 2026-09-22 [env] W8-A COMPLETE: production ENABLE_NEW_PROGRAM_STATE=true ENABLE_NEW_PROGRAM_STATE_WRITES=true ENABLE_LEGACY_PROGRAM_MEMBER_MIRROR=true W8A_CUTOVER_TIME=2026-09-22T05:22:56.000Z; ProgramEnrollment pe_pm_ is ProgramMember-domain current-state+write authority; ProgramMember remains compatibility mirror; dual-write stays; W8-B not started
- 2026-09-22 [schema|env|convention] W8-A ProgramMember current-state: ENABLE_NEW_PROGRAM_STATE (default off) reads PE pe_pm_ snapshots for membership/unlock/skip/scores/recommendations; ENABLE_NEW_PROGRAM_STATE_WRITES (default off) commits ProgramEnrollment first; ENABLE_LEGACY_PROGRAM_MEMBER_MIRROR (default on) keeps ProgramMember as compatibility writes; identity stays CandidateProfile; recruiter permission stays CandidateVisibility; dual-write stays; W8-B not started
- 2026-09-22 [env] W7-B COMPLETE: production ENABLE_LEGACY_ENROLLMENT_DENORM_MIRROR=false W7B_CUTOVER_TIME=2026-09-21T23:40:27.000Z; Enrollment days/streaks/lastSubmittedDay and StudentProfile.domain frozen; ProgramEnrollment + ActivityAttempt remain canonical; operational Enrollment fields stay live; dual-write stays; W8 not started
- 2026-09-22 [env|convention] W7-B Enrollment progress denorm freeze: ENABLE_LEGACY_ENROLLMENT_DENORM_MIRROR=false stops Enrollment.daysCompleted/lastSubmittedDay/currentStreak/longestStreak and StudentProfile.domain compatibility writes; operational Enrollment status/startedAt/completedAt stay live; ProgramEnrollment + ActivityAttempt remain canonical; dual-write stays; W8 not started
- 2026-09-22 [env] W7-A COMPLETE: production ENABLE_NEW_ENROLLMENT_STATE=true ENABLE_LEGACY_ENROLLMENT_DENORM_MIRROR=true W7A_CUTOVER_TIME=2026-09-21T20:22:43.000Z; live enrollment/progress/domain current-state from ProgramEnrollment snapshots + ActivityAttempt days; Enrollment denorms and StudentProfile.domain remain compatibility mirrors; W7-B/W8 not started
- 2026-09-22 [schema|env|convention] W7-A Enrollment denorm current-state: ENABLE_NEW_ENROLLMENT_STATE (default off) reads AA-derived days + ProgramEnrollment.trackCurrentStreak/trackLongestStreak snapshots + first-track domain; ENABLE_LEGACY_ENROLLMENT_DENORM_MIRROR (default on) keeps Enrollment days/streaks and StudentProfile.domain as compatibility writes; EnrollmentProgress not authoritative; W7-B/W8 not started
- 2026-09-22 [env] W6-B COMPLETE: production ENABLE_LEGACY_PROGRESS_MIRROR=false W6B_CUTOVER_TIME=2026-09-21T19:30:49.000Z; ActivityAttempt+ActivityEvaluation are sole progress current-state+write authority; Submission/QuizAttempt/ProgramMissionSubmission frozen; Enrollment/domain stay later; dual-write stays; W7 not started
- 2026-09-22 [env|convention] W6-B Progress legacy-mirror freeze: ENABLE_LEGACY_PROGRESS_MIRROR=false stops Submission/QuizAttempt/ProgramMissionSubmission writes; GitHub uniqueness uses ActivityAttempt payload unique index; Enrollment daysCompleted/streaks still written from canonical attempts (W7 not started); dual-write stays; W7 not started
- 2026-09-21 [env] W6-A COMPLETE: production ENABLE_NEW_PROGRESS_WRITES=true ENABLE_LEGACY_PROGRESS_MIRROR=true; ActivityAttempt+ActivityEvaluation are progress write authority; Submission/QuizAttempt/ProgramMissionSubmission remain compatibility mirrors; Enrollment/domain stay later; dual-write stays; W6-B/W7 not started
- 2026-09-21 [env|convention] W6-A Progress write authority: ENABLE_NEW_PROGRESS_WRITES (default off) commits ActivityAttempt+ActivityEvaluation before Submission/QuizAttempt/ProgramMissionSubmission mirrors; ENABLE_LEGACY_PROGRESS_MIRROR (default on); Enrollment/domain stay later; dual-write stays; W6-B not started
- 2026-09-21 [env] W5-B COMPLETE: production ENABLE_LEGACY_AMBASSADOR_MIRROR=false W5B_CUTOVER_TIME=2026-09-21T17:51:14.000Z; CampusAmbassadorApplication is sole ambassador current-state+write authority; StudentProfile ambassador fields frozen (anonymize still scrubs); dual-write stays; W6 not started
- 2026-09-21 [env|convention] W5-B StudentProfile ambassador freeze: ENABLE_LEGACY_AMBASSADOR_MIRROR=false stops apply/dismiss SP ambassador mirrors; CampusAmbassadorApplication is sole current-state source; anonymize wipe still scrubs frozen SP ambassador snapshots; dual-write stays; W6 not started
- 2026-09-21 [env] W5-A COMPLETE: production ENABLE_NEW_AMBASSADOR_WRITES=true ENABLE_LEGACY_AMBASSADOR_MIRROR=true; CampusAmbassadorApplication is ambassador write authority; StudentProfile ambassador fields remain compatibility mirrors; dual-write stays; W5-B/W6 not started
- 2026-09-21 [schema|env|convention] W5-A Ambassador write authority: CampusAmbassadorApplication is the canonical candidacy model; ENABLE_NEW_AMBASSADOR_WRITES (default off) commits it before the StudentProfile ambassador compatibility mirror; ENABLE_LEGACY_AMBASSADOR_MIRROR (default on); dual-write stays; W5-B/W6 not started
- 2026-09-21 [env] W4-B COMPLETE: production ENABLE_LEGACY_STUDENT_PROFILE_MIRROR=false W4B_CUTOVER_TIME=2026-09-21T16:45:57.000Z; Candidate models are sole identity/referral current-state source; StudentProfile identity/referral fields frozen; SP table remains for later families; dual-write stays; W5 not started
- 2026-09-21 [env|convention] W4-B StudentProfile identity/referral freeze: ENABLE_LEGACY_STUDENT_PROFILE_MIRROR=false stops W4-owned SP identity mirrors; Candidate models are sole current-state source; SP table stays for later families; dual-write stays; W5 not started
- 2026-09-21 [env] W4-A COMPLETE: production ENABLE_NEW_CANDIDATE_WRITES=true ENABLE_LEGACY_STUDENT_PROFILE_MIRROR=true; CandidateProfile/structured tables are identity/referral write authority; StudentProfile identity fields remain compatibility mirrors; dual-write stays; W4-B/W5 not started
- 2026-09-21 [env|convention] W4-A Candidate identity write authority: ENABLE_NEW_CANDIDATE_WRITES (default off) commits CandidateProfile/structured tables before the StudentProfile identity/referral mirror; ENABLE_LEGACY_STUDENT_PROFILE_MIRROR (default on); dual-write stays; W4-B/W5 not started
- 2026-09-21 [env] W3-B COMPLETE: production ENABLE_LEGACY_CERTIFICATE_MIRROR=false W3B_CUTOVER_TIME=2026-09-21T15:18:05.000Z; Credential is the sole issuance write model; Certificate is frozen historical; dual-write stays; W4 not started
- 2026-09-21 [env|convention] W3-B Certificate mirror shutdown: ENABLE_LEGACY_CERTIFICATE_MIRROR=false freezes Certificate; /hire completion uses Credential (pe_enr_ enrollment source); admin provenance keeps historical Certificate.id reads only; dual-write stays; W4 not started
- 2026-09-21 [env] W3-A COMPLETE: production ENABLE_NEW_CREDENTIAL_WRITES=true ENABLE_LEGACY_CERTIFICATE_MIRROR=true; Credential is the issuance write model; Certificate remains the compatibility mirror; dual-write stays; W3-B/W4 not started
- 2026-09-21 [env|convention] W3-A Credential write authority: ENABLE_NEW_CREDENTIAL_WRITES (default off) commits Credential before the Certificate compatibility mirror; ENABLE_LEGACY_CERTIFICATE_MIRROR (default on); dual-write stays; W3-B/W4 not started
- 2026-09-21 [rule] Recruiter registration uses one emailed 6-digit code to verify the address and open the session; a second sign-in OTP is recovery only (failed session or a restored draft with no cookie).
- 2026-09-21 [env] W2 COMPLETE: production ENABLE_NEW_VISIBILITY_WRITES=true ENABLE_LEGACY_VISIBILITY_MIRROR=true; CandidateVisibility is the sole recruiter-discovery write/read gate; recruiterVisibilityConsentAt retained as historical label only; dual-write stays; W3 not started
- 2026-09-21 [env|convention] W2 recruiter visibility: CandidateVisibility is the sole write boundary (applyVisibilityChange); ENABLE_NEW_VISIBILITY_WRITES (default off) commits CV before the dual-write savepoint; recruiterVisibilityConsentAt is historical label only, not a gate; V4 accepts platform_default_profile; dual-write stays; W3 not started
- 2026-09-21 [env] W1-B COMPLETE: production ENABLE_LEGACY_POINTS_MIRROR=false W1B_CUTOVER_TIME=2026-09-21T11:07:46.000Z; PointsTransaction sole ledger, PointsAccount sole wallet; User/SP/SynergyEvent frozen; dual-write stays; W2 not started
- 2026-09-21 [env|convention] W1-B Points legacy-mirror switch ENABLE_LEGACY_POINTS_MIRROR (default on); applyPointsChange skips User/SP/SynergyEvent when false; recon/drift treat User/event diffs as informational in that mode; dual-write stays on; W2 not started
- 2026-09-18 [rule] Legal proprietor and Grievance Officer name on /terms, /privacy, /contact is Suman Shukla (Divya Shukla removed).
- 2026-09-18 [convention] Plan 133 D-4: the recruiter-assessment sendable pool carries a project scope. `AssessmentStore.listAssignableCandidates(recruiterUserId, { projectId })`, `listSendableCandidates` and `getAssessmentMonitor` take an optional `AssignablePoolScope`; `projectId` set = that project's SHORTLISTED `TalentRequestMatch` rows only (new `listProjectShortlistForProject`, ownership in the WHERE), absent/null = the legacy `RecruiterShortlistItem` list only — the same two cases as `scopePodRows`, never a union of projects (that union was the bug: one shortlisted candidate in the pod, everyone in the checkboxes). Carried by `?projectId=` on `/hire/create-test` (pod CTA, preset Customize, Templates back-link) and by an optional `projectId` on `assignAssessmentSchema` / `createAndSendSchema` / `createAndSendFromPresetsSchema`, so the server resolves refs against the same pool it rendered. `/hire/assessments/[assessmentId]` prefers `?projectId`, else the existing `TalentProjectAssessment` link (new read-only `findProjectIdForAssessment`; no new linking). `listProjectShortlist` (recruiter-wide) kept but now has no caller.
- 2026-09-18 [rule] Legal proprietor and Grievance Officer name on /terms, /privacy, /contact is Divya Shukla (was Suman Shukla).
- 2026-09-17 [rule] Recruiter search reads the role (plan 147 follow-up): `spec.title` was stored and never used, so every title-only search returned the same people in the same order. New pure `features/hire/role-match.ts` parses the role and scores title fit against cohort jobRole + CandidateProfile.headline + CandidatePreference.preferredRoles + CandidateExperience titles (loaded once per search by `attachRoleTitles` in track-loaders.ts via `loadRoleTitleSources` in repositories/talent.ts; searchCandidates and the search-qa probe both call it). New ScoreDimension `role` (base weight 20, zero unless a title is given, so untitled searches score exactly as before); with no skills named the role's typical skills (catalog groups + named skills) replace the neutral 0.5 stack; tierFor caps at PARTIAL when there is no title connection and no typical skill (no must-haves named). Rank-only: never excludes, but unrelated candidates can fall to NONE and NONE only pads a page with <5 better matches. EvidenceCoverage.dimensions and ScoreBreakdown gained `role` (optional on stored breakdowns). search-qa: role is a RANK_ONLY registry entry (was NOT_IMPLEMENTED), golden QA067–QA071, `roleTitles` drift, CanonicalPreference.preferredRoles.
- 2026-09-17 [rule] Usable profiles get a discovery record (plan 117 follow-up): new `repositories/discovery-record.ts` creates `CandidateVisibility {searchableByRecruiters: true, consentSource: "platform_default_profile"}` after `saveBasicInfo` / `saveSkillClaims` commit when the profile is usable (non-blank name + ≥1 claimed skill, STUDENT, not deleted/disabled) and NO row exists. Create-only (`createMany skipDuplicates`), never updates hidden/withdrawn/closed rows, failure never fails the save. Before this only challenge/cohort enrolment created rows, so 11 of 25 profiles built since 2026-09-01 were invisible. Added to the allowed CandidateVisibility writers in visibility.test.ts. The ~2,150 historical rows closed by 2b are untouched (product/legal decision); the 12 existing no-row usable profiles get their row on their next name or skill save.
- 2026-09-17 [rule] Recruiter skill matching folds two spelling differences that never change the skill: "&" = "and" and plurals (sameSkill → spellingKey in score-candidate.ts; plural fold only on 5+ letter words and never after s/j/u/i). The candidate-data audit now separates catalog spelling clusters search still does not match (NORMALIZATION_ERROR) from duplicate catalog rows search already matches (INFO, for prisma/scripts/dedupe-skills.ts).
- 2026-09-17 [rule] Recruiter search ranking and saved results (plan 147, QA-KI-006/007/008/010/011): rankCandidates lists by tier (STRONG, PARTIAL, NONE) before score, so evidence-free profiles no longer outrank proven work; new pure selectSearchResults (score-candidate.ts) ranks the WHOLE pool before picking the page (the 100-candidate window cut matches before the must-have gate) and keys near misses by candidateRef (programMemberId dropped every non-cohort candidate); searchCandidates and the search-qa probe both call it, RANK_WINDOW removed. loadRequestMatches: savedMatchRef validates stored sources against the track registry (PROFILE matches were rendered as CLAUDE refs and could not be shortlisted) and saved lists order tier, score, firstSeenAt, candidateUserId. pool-brief stack parser uses explicit edges instead of \b so "c++" parses. Admin discoverability panel (get-candidate-discoverability.ts) counts only tracks the loaders carry: HIRE_CHALLENGE_POOL floor via hireChallengePool(), ENROLLED/COMPLETED members of resolvePoolCohorts(); profile pool cap imported from CHALLENGE_POOL_CAP. search-qa known-issues registry is now empty; the full audit runs the real panel loader for every searchable candidate.
- 2026-09-17 [rule] Recruiter skill/city matching (plan 147, QA-KI-004 + QA-KI-009): splitSkills keeps catalog skill names and short compounds ("AI/ML", "C/C++", "Git & GitHub", "UI/UX Design") whole instead of splitting at / & and — pastes (, ; | or a 4+ word part) split exactly as before; stackTokensMatch matches a compound part by equality ("ML" still finds "AI/ML") and folds both sides through skill-catalog canonicalSkillName plus symbol-safe squash equality (golang = Go, reactjs = React, cpp = C++; C/C++/C# never collide); evaluateHardFilters folds unambiguous city renames/typos (Bangalore/banglore = Bengaluru, Gurgaon = Gurugram) and never merges NCR cities. Card chips now show a compound claim as one chip. The search-qa oracle gained the matching compound-part rule. Also pinned QA-KI-011: the admin Recruiter search panel (candidate-discoverability.ts, T-265) reports tracks the loaders do not load.
- 2026-09-17 [convention] Recruiter onboarding is now one wizard at /recruiter-onboarding (welcome → You → Company → Verify → sign-in code → ready → /hire); /recruiter-onboarding/signup opens it at "You", /discover /connect /assess redirect to it, and the four story screens, the photo frame and the quote-panel auth frame are gone (signin keeps its logic in the new shell). Account rules unchanged: same requestRecruiterOtpAction/registerRecruiterWithOtpAction, recruiterRegSubmitted now emitted from recruiter-onboarding-wizard.tsx, and the sign-in code is requested automatically once the account exists. Draft kept in sessionStorage key `abtalks-recruiter-onboarding` (never the code; consent not restored). Optional company fields (website/industry/size/HQ) are saved after sign-in through updateRecruiterProfileAction, reading the stored company name first so a VerifiedRecruiterSeat name is never overwritten. Motion system centralised in components/recruiter-onboarding/motion.ts.
- 2026-09-16 [convention] T-266: admin candidate Skills + Evidence cards now trace every evidence badge to the row that earned it. New pure buildEvidenceProvenance (features/admin/evidence-provenance.ts) + loader getEvidenceProvenance follow SkillEvidence.sourceId (AssessmentScore, ActivityEvaluation, Credential, HackathonParticipant/Team), Credential.sourceKey (ProgramEnrollment, "<teamId>:<certId>" HackathonTeam, Certificate, AssessmentReport) and CandidateAchievement.sourceId (ProgramEnrollment, HackathonParticipant, WorkshopRegistration, AssessmentReport, Credential) into their own tables; a badge is traced only when that row exists, belongs to the candidate, still meets its writer bar (authoritative + passed evaluation, ISSUED credential) and has its own date, and the source name/date come from that row, never the badge snapshot (so HACKATHON credentials now read "ViCoDathon 2026 · Team X, Submitted <date>"). Everything else renders Not traced with the reason. The Evidence-backed skill chip now counts traced sources instead of cached CandidateSkill.verified/evidenceCount. Programme-verified skills get dated origins via new getVerifiedSkillsWithOrigins (cohort completedAt, or the submission that made the 50th completed day, read from the same rows/flag as getChallengeProgressStats); getVerifiedSkills keeps its candidate-facing shape. The admin assembler no longer loads getProfileEvidence/getVerifiedSkills (nothing on the page read them). npm run test:evidence-provenance.
- 2026-09-16 [convention] T-265: every row of the admin Assessments card gets a View details link to /admin/students/[id]/assessments/[assignmentId] — a read-only admin page showing the real attempt (assigned/started/submitted, end reason, who assigned it), the result (stored scorePercent/passed against the pass mark, plus a recount of the saved answers), every answer with the candidate's picks, paragraph text and pasted link against the answer key, and the T-219 recorded activity (same SUMMARY_COPY, disclaimers and timeline the recruiter attempt page uses). On top of it an explanation that accounts for the outcome and calls out what makes a result unreliable: no score written, a stored score that no longer matches the answers, an MCQ published with no correct option, required questions or over-limit paragraphs (both of which mean the server closed the attempt), and strict-mode auto-ends. Per-question grading is the new shared `gradeQuestion`/`scoreAnswers` in `assessment-attempts/service.ts` (finishAttempt and finishAttemptForced now call it, scoring behaviour unchanged) so the breakdown cannot disagree with the stored score. The read is scoped `{ id: assignmentId, candidateUserId }`, and the page is held to `BANNED_CLAIM_PATTERN` like every other attempt surface.
- 2026-09-16 [convention] Auth.js PKCE/state/nonce cookies versioned to .v2 and scoped to .abtalks.in so leftover Google verifiers (account switch / www vs apex) no longer 500 /api/auth/error.
- 2026-09-16 [convention] Admin candidate page: Profile/Diagnosis toggle beside the avatar (default Profile); ops collapsed into a top-right Perform admin action dropdown that confirms each action (T-272 reason and T-217 type-delete unchanged).
- 2026-09-16 [convention] T-264: admin candidate detail at /admin/students/[id] assembles the candidate's own profile, skills (self-declared vs evidence-backed), evidence, applications, assessments (status only), programmes, notification deliveries and account state; search and Candidates list already open this route.
- 2026-09-16 [convention] /admin/students/[id] now opens with a "Recruiter search" panel answering why a candidate does or does not appear: it lists every condition the recruiter-search path really applies — deletion, suspension, admin moderation stop (plain + durable), presence of the CandidateVisibility record the gate joins to, profile usability (row, name, headline, location, education, experience), skills claimed and skill evidence, track-pool membership and the 600-row profile-pool cap — and names the first one that actually blocks. The verdict is two live probes, `searchableUserWhere()` and `resolveProfileRefs([userId])`, so it cannot drift from the live query; the panel is read-only and reads only the three gate columns. Copy deliberately carries NO consent / visibility / opt-out wording, because no candidate-facing switch over discovery exists (plan 133 + visibility.test.ts) — `npm run test:candidate-discoverability` pins both the blocker selection and the banned wording.
- 2026-09-15 [convention] Dashboard sidebar open/collapsed is click-only (no hover expand); brand row uses /sidebar-toggle.webp instead of cropped AB / three-dots.
- 2026-09-15 [rule] T-224 Career Guidance algo v2: no jobs; progression.json edges (pool 8); days≥50 / Claude credential gates; slot daily mix (≤4) + one localStorage refill; soft-dedupe vs ACTIVE enrollments.
- 2026-09-16 [rule] Recruiter search filter fixes found by the plan-147 audit: evaluateHardFilters now folds BOTH sides of the work-mode comparison through normalizeWorkMode (the /profile picker stores "Remote"/"On-site" labels, the spec carries REMOTE/ONSITE enums — every candidate with a stated work mode was excluded from every work-mode search; an unparseable value now reads as unstated instead of a mismatch), treats a 0/0 "not decided" budget as no budget (effectiveBudget) and the "Any"/"anywhere" city sentinel as no city (effectiveCity); repositories/talent.ts orders education graduationYear desc NULLS LAST at both call sites so a year-less row no longer hides the entered year. Measured live impact before the fix: 9 candidates hidden across 43 of 179 audited searches, 6 documents with a null graduation year. QA-KI-001/002/003/005 removed from search-qa/known-issues.ts and their tests are now ordinary assertions.
- 2026-09-15 [convention|rule] Plan 147: recruiter search QA system in src/features/search-qa (canonical-DB oracle vs the real searchCandidates pipeline, filter registry, pairwise/covering-array combinations, in-memory golden dataset, strict known-issues registry QA-KI-001..010, data-quality + normalization audits, explain mode) + read-only repositories/search-audit.ts + /admin/search-health + .github/workflows/recruiter-search-qa.yml. Commands: test:recruiter-search (offline, CI), audit:recruiter-search | audit:search-index | audit:candidate-data | audit:search-explain (force default_transaction_read_only=on on DIRECT_URL; --profile=production fills ENABLE_NEW_TALENT/HIRE_CHALLENGE_POOL/HIRE_OPEN_COHORT_IDS gaps). search-candidates.ts now exports MIN_RESULTS / CHALLENGE_POOL_CAP / RANK_WINDOW and repositories/hire.ts exports HACKATHON_POOL_TAKE (no behaviour change). No schema change.
- 2026-09-15 [convention] T-207: Platform Admin console IA (Overview, Search, Candidates, Recruiters, Credits, Assessments, Jobs, Communications, Audit, Delivery, Settings) wired to existing admin data. View As, SMS, and team plans are not shipped.
- 2026-09-15 [schema|rule] T-272/T-270/T-277/T-217: User disable+sessionInvalidatedAt; AdminAction actor snapshot + nullable FKs SetNull; candidate-owned Restrict→Cascade and credit/engagement candidate FKs SetNull; runtime mock allowance/cost; candidate hard-delete. Operator never sees a password.
- 2026-09-15 [rule] Recruiter assessment templates live on /hire/create-test; using a template publishes and assigns to ticked Shortlist candidates (preset ids + refs only). Customize still opens the locked builder. /hire/assessments is the list only.
- 2026-09-15 [rule] Scout inspector non-contact reads (work history, External profiles, ABTalks Evidence) use resolveInspectorCandidate without challenge minDays; 50-day completion gate uses Math.max(attempts, Enrollment.daysCompleted); LeetCode/CodeChef bare hosts get https:// on save/display.
- 2026-09-15 [convention] Scout inspector ABTalks Evidence lists pool-gated completions and hackathon placements only (50+ challenge days including Claude, finished ProgramEnrollment cohorts, ViCoDathon Winner/Runner Up/Second Runner Up/Top 5); in-progress track counts are not shown.
- 2026-09-14 [convention] T-224 hub Career Guidance is a daily 4-card mix (profile recs first, then catalog check-ins/quotes with once/daily/weekly cadence); dismissals live in localStorage for the IST day and are not activity/evidence.
- 2026-09-14 [rule] T-224: candidate dashboard Career Guidance recommends cohorts, hackathons, challenges, jobs and mocks only from the candidate's own profile and activity — no invented statistics.
- 2026-09-14 [rule] T-216: recruiter View Detail may show declared GitHub/LeetCode/CodeChef URLs as SELF-REPORTED (no OAuth/API sync; email/phone/linkedinUrl/resumeUrl still unlock-gated).
- 2026-09-14 [rule] Plan 141: strict STARTED assessments end on leave (no resume) — in-app nav blocked until Submit; pagehide keepalive POST /api/assessments/[assignmentId]/leave force-finalizes incomplete answers; loadAttempt safety net on PAGE_LEFT; clipboard stays blocked without toast.
- 2026-09-14 [schema|rule] T-219 (plan 140): strict mode for recruiter assessments — RecruiterAssessment.strictMode (set true by publish only; pre-existing rows false) + cameraRequired (builder, DRAFT-editable); AssessmentAttemptSession (per page lifetime, server firstSeenAt/lastSeenAt) + AssessmentAttemptEvent (16 facts: fullscreen/visibility/focus/page leave/clipboard/upload link/camera; unique assignment×session×seq; composite FK to session, CASCADE; RESTRICT to question) via POST /api/assessments/[assignmentId]/events (route handler: sendBeacon/keepalive need it; SameSite=Lax + Origin check; one nested upsert, no interactive tx); device clock corrected per batch; strict attempts refuse phones (client gate + Sec-CH-UA-Mobile on start/save/submit) and browsers without fullscreen; fullscreen exit or camera stop blurs + blocks; copy/cut/paste/drop blocked except paste into file-link fields; recruiter /hire/assessments/[id]/attempts/[assignmentId] shows facts, time away (union of not-in-view across sessions) and fixed disclaimers — no detection or cheating claims (tested).
- 2026-09-12 [convention] Hub search is global except /hire; placeholder Search ABTalks.
- 2026-09-12 [convention] Scout inspector Experience is CandidateExperience (typed or resume-merged); Claude/Platform track proof moved to ABTalks Evidence.
- 2026-09-12 [rule] Recruiter can open the Scout inspector on owned-job applicants (PROFILE ref); unlock/message reuse T-229/T-232 with owned-application as a second addressability check — email/phone stay off the list and card payload.
- 2026-09-12 [rule] Recruiter can list applicants for owned jobs only (T-226 NOT_FOUND on a foreign id); payload is display name + note + status + applied date — email/phone stay off the select.
- 2026-09-12 [schema] T-245/T-246 Job lifecycle columns (status, workMode, skills, recruiterId, publishedAt, closedAt) and JobApplication.status/resumeUrl/coverLetter/updatedAt now have migration 20260912140000 — they were in schema.prisma without a migration, which crashed /jobs with P2022.
- 2026-09-12 [rule] Hire inspector LinkedIn URL is released only after CONTACT_SHARED, through loadProtectedContact (same gate as email/phone); the search/match payload still carries linkedinConnected only.
- 2026-09-12 [convention] PRs must fill `.github/PULL_REQUEST_TEMPLATE.md`: domain, owner, cross-module, shared/platform files, Sohail review, test evidence, and the ten-question self-audit.
- 2026-09-12 [convention] Issues opened from `.github/ISSUE_TEMPLATE/module.md` get `module:*` / `primary:*` from ticked checkboxes (first tick is primary); issues without those boxes are left unlabeled.
- 2026-09-12 [schema|rule] Plan 133: projects (TalentRequest) now hold SEARCH SESSIONS. New `TalentSearchSession` (unique requestId+ordinal; own spec, resultCandidateIds, resultSnapshot), `TalentRequestMessage.sessionId` (nullable), `TalentProjectAssessment` (assessmentId unique; files an assessment under a project without touching T-243/T-244 — no row = Unassigned). Migration `20260912090000_project_search_sessions` (additive, no SQL backfill). `ensureLegacySession` files pre-session history as Session 1 on open/send/search; `db:backfill:search-sessions` does it up front. `sendScoutMessageAction`/`runMatchAction` take optional `sessionId` (absent = new session); TalentRequest spec columns still mirror the latest brief. Run prune now spares UNDECIDED rows any session shows (`pruneUndecidedOutsideSessions`). New `createTalentProjectAction` (named project, no session). Header shortlist scoped to the open project via `listProjectShortlistByProject` + `shortlist-scope.ts`; legacy RecruiterShortlistItem shown only off-project; `listProjectShortlist` unchanged (assessment builder). `/hire/[id]?session=`; sidebar lists sessions, project assessments, projects.
- 2026-09-12 [convention] PRs auto-labeled from changed paths: `module:<id>` for every matching module and `primary:<id>` for the largest file share (tie-break: changed lines); map lives in scripts/pr-module-labels.mjs.
- 2026-09-12 [convention] After `/hire` search, Scout chat is hidden; recruiters edit profile+ranker filters in a dialog and re-run search without an agent turn (plan 133).
- 2026-09-12 [convention] Recruiter `/hire` type aligned to Design System v2: Outfit headings only (H1 64/70, H2 40/48, H3 24/30, H4 20/26), Inter UI (nav 14/20/400 uppercase, sidebar 14/20/500, input 16/24, button 16/20/600); dropped weights 300/450/800.
- 2026-09-11 [rule] T-258 UNLOCK rate limit now wraps unlockContactAction (T-229 spend path); preview/reveal stay reads and do not consume the bucket.
- 2026-09-11 [convention] Cursor rules split for chat use: always-apply ABTalks88 contract + Sohail ownership; forest-green UI is glob/agent; workbook moved to `.cursor/sources` because xlsx is not a Cursor rule.
- 2026-09-11 [schema|rule] Profile completion is field-weighted to 100% (plan 132): gated Experience/Education on entry #1, skills 0/5/10, resume 3%, extra rows add 0; `CandidateProfile.hasNoWorkExperience` awards the Experience 20% for freshers (migration 20260911220000, additive, NOT applied). Completeness stays a UX number in `computeCompleteness` and gates nothing.
- 2026-09-11 [rule] Plan 131 (written as 130, renumbered when master's presets plan 130 merged): the assessment builder's Create saves, publishes and assigns in one step (createPublishAndAssign → createAndSendRecruiterAssessmentAction, workspace-gated, existing assessment.assigned notifier) to candidates the recruiter ticks from the live Shortlist (listSendableCandidates: legacy + T-149 project halves, searchable, refs only — no user ids to the client; /hire/create-test now uses it instead of legacy-only getShortlist, so shortlistRefs covers both halves); refs are checked before anything is written, a publish failure returns the draft id so the retry updates it, an assign failure after publishing is reported (assignError) and hands over to /hire/assessments/[id]; Edit/Preview toggle hidden at ≥1100px; new MCQ options start empty with "Option N" placeholders. No schema change.
- 2026-09-11 [schema|rule] T-218 (plan 129): AssessmentAnswer (one row per assignment×question, CASCADE on assignment, RESTRICT on question; migration 20260911210000, creates-only, NOT applied to any database) + candidate routes /assessments (list) and /assessments/[assignmentId] (instructions → taking → submitted), both in middleware protectedPaths, and an "Assessments" candidate sidebar item; start/save/submit guarded on RecruiterAssessmentAssignment.status exactly as plan 128 §10 (scalar-column guards only, writes via writeClient()), each save and the submit take the assignment row lock first so no answer lands after a submission and a second submit matches nothing; submit scores MCQ (exact-set match) onto scorePercent/passed in the same transaction and rolls back when a required question is unanswered or a paragraph is over its word cap; every change autosaves (no localStorage); the candidate sees "Submitted" only — isCorrect/scorePercent/passed never reach candidate pages or responses; CandidateAssessmentScreen extended (not forked), its styles moved from hire-scout.css to candidate-assessment-screen.css, recruiter upload links rendered only when http(s).
- 2026-09-11 [schema|rule] T-244 (plan 128): RecruiterAssessmentAssignment (one row per assessment×candidate, RESTRICT on assessment, CASCADE on candidate; migration 20260911180000, creates-only, NOT applied to any database) + publish (DRAFT→PUBLISHED, idempotent via the DRAFT guard in updateMany's WHERE, needs ≥1 MCQ worth points), assign from the live Shortlist union (legacy + T-149 project, re-filtered for searchability, never shortlistRefs; client sends refs only, whole call refused if any ref is off the Shortlist; ≤25 per call) with one assessment.assigned notification per candidate per assessment via dispatch() (primaryEntityId = assessmentId, fixed copy, sequential, every row so a failed send retries), DRAFT-only delete, /hire/assessments/[id] monitor, real Students/Passed/Failed; T-218 owns status/startedAt/submittedAt/scorePercent/passed and the /assessments/[assignmentId] route.
- 2026-09-11 Plan 127 (part 10): the search bar opts out of screen 2's zoom (`.hire-app--results .scout-composer { zoom: calc(1/--hire-zoom) }`) and takes screen 2's sizes as design x zoom in real px, so it is in one coordinate space on both screens: the hero keeps its larger (pic-3) size and field/type/button TRANSITION to the dashboard's (1200ms) while it stretches. New project now plays Search in reverse in one move: `ghostFadeOut()` (hire-stage-flip.ts) pins static clones of screen 2's toolbar/thread/nav card/header buttons in a layer carrying the stage classes and fades them while the reset sends the bar back up; signed-in projects navigate to /hire after `HIRE_STAGE_MS`. Result cards: padding 24x21 -> 16x18, CTA gap 32 -> 18, corner buttons 36/13px -> 42/14px.
- 2026-09-11 Plan 127 (part 9): `.hire-app--results .scout__body` (>=1101px) sets `transition: none` — the base 280ms grid-template-columns transition started from one full-width column on arrival from screen 1, so the bar was measured and stretched to full width, then snapped to the card column.
- 2026-09-11 Plan 127 (part 8): search bar now STRETCHES between screens — `playStageFlip` animates translate + width/maxWidth (no scale), and the hero bar is drawn at screen 2 values x `--hire-zoom` so field, type and button are identical on both sides; the hero glass card fades out (bg/shadow/padding, 1200ms). Screen 2 always reserves the profile column (>=1101px), so cards no longer run full width. Header: Save for Later / Shortlist / credits / Assessments hidden on screen 1 again (the master merge had dropped the `!isLanding` guard); JPG icons replaced by lucide SVGs (Bookmark, UserCheck, ClipboardCheck); screen-2 header buttons 32->38px. Sidebar placeholder squares replaced by lucide icons (House, FolderKanban, ChartColumn, FolderOpen, LifeBuoy).
- 2026-09-11 Plan 127 (part 7): screen 1 suggestion lines are white (were #0b464f / #000, near-invisible on the green) and turn like cubes: `RecruiterSearchSuggestions` renders an outgoing + incoming `.rsearch__face` per line during a `roll` (from, dir, id); `rsearch-cube-in/out` rotateX 90deg about an axis 0.6em behind the text, 700ms (`CUBE_MS`), reversed when scrolled back. Slot styles retargeted from `button` to `.rsearch__face` so both faces match. Still hidden <=900px (the mobile frame has none).
- 2026-09-11 Plan 127 (part 6): stage timing slowed and unified — `HIRE_STAGE_MS = 1200` (hire-stage-flip.ts) for the bar FLIP, matched by every field/shade/glow colour transition, the header glass and a wordmark crossfade (both wordmarks now stacked in `.hire-app__logo-swap` on desk routes instead of an <Image> swap, which changed a frame ahead of the background); screen-2-only header buttons fade in. Hero exit 520ms, return beat 380ms, workspace in 720ms@380ms, cards 560ms. Ambient drift ~30% faster (20-30s swings). Portrait draws every field ellipse (glows and dark layers) as vmax circles so phones get round glows, not streaks.
- 2026-09-11 Plan 127 (part 5): New search now stays on screen 2 (`freshChat` keeps the stage at results with an empty thread; only New project returns to screen 1). The field is rebuilt as base colour + four moving light-green blob elements (`.hire-field__glow--a..d`, the export ellipses at their design size/position) interleaved with two static dark layers (`.hire-field__shade--low/--high`) in the ORIGINAL gradient paint order, so screen 1 is exactly as bright as the design while the blobs roam (+/-30vw/vh Lissajous drift, scale 0.96-1.12, opacity shimmer 0.8-1). Replaces the earlier extra faint glows, which read as static.
- 2026-09-11 Plan 127 (part 4): New search vs New project are separate intents. Desk context has `newSearchNonce`/`requestNewSearch` (same project: ScoutChat `newSearch()` clears the thread, keeps `requestId` and guest search history, writes a not-searched guest session) and new `newProjectNonce`/`requestNewProject` (`newProject()`: `/hire` for a saved project, full guest wipe). The sidebar "+ Create New Project" now calls `requestNewProject` (it previously bumped the search counter, whose reset was project-level). Both buttons sit in `.scout__toolbar` beside Filters. `hero`/chrome `landing` are no longer gated on `/hire`/`initialRequestId`, so a project page can return to screen 1. Screen 2 -> 1 runs in two beats (`is-returning` 220ms fade, then reset + reverse FLIP from the recorded screen-2 bar + morph back to green + `is-arriving`). Screen 1 gets an ambient glow: `.hire-field__glow` x/y/scale nested layers, eased alternate at unrelated periods (Lissajous), transform-only, paused on screen 2.
- 2026-09-11 Plan 127 (part 3): screen 2 renders at design scale. `hire-zoom.ts` computes `--hire-zoom` = viewport/1920 (floor 0.62, never below 901px), set on <html> by a blocking inline script in `app/hire/layout.tsx` (no full-size flash) and kept current on resize by HireChrome; `.hire-app--results` applies `zoom` with the shell heights divided back out, and re-shows `.hire-side`, which the <=1365px viewport rule hid although the zoomed layout has room. `playStageFlip` divides its translate by `currentCSSZoom` (the transform lives in the zoomed space). Leaving hero pieces cancel the zoom so they stay pinned at their measured rects.
- 2026-09-11 Plan 127 (part 2): screen 1 is now a STAGE of ScoutChat, not an early-returned page. `RecruiterSearchLanding` split into `RecruiterSearchTitle` + `RecruiterSearchSuggestions`; the desk composer is the one search bar on both screens (same DOM node, kept in one tree slot), moved by `hire-stage-flip.ts` at the moment Search is pressed. `hero = !talked`, so the stage flips on the press while the request is in flight; the chrome `landing` flag is pushed from a layout effect (was an effect, which let the chrome switch a painted frame late) and the FLIP keys on it. `.hire-field` is one gradient whose blob colours are registered `@property` values morphing green -> #f4f4f5 (replaces the opacity crossfade). Skeleton cards (`.hire-skel`) hold the workspace until the backend returns. Textarea autosize also refits on width change (a measurement taken while narrow was kept at 132px). No action, ranking, repository or auth change.
- 2026-09-11 Plan 127 (part 1, on top of the landing + dashboard from f3ad055c/4b568d93): the two Hire screens now hand over instead of swapping. `.hire-field` is a fixed gradient layer rendered by HireChrome and cross-faded (720ms) between landing and results, replacing `recruiter-search-blobs.svg` — that export painted an opaque backdrop that left a black strip under the card at desktop aspect ratios and hid suggestion slots 1-2 (near-black type that only reads on the green). ScoutChat keeps the landing until a search returns matches (`showLanding = view === "scout" && !initialRequestId && !searched`, and the context `landing` flag is the same expression, not a second copy of the rule) — so an ordinary reply like "hi" stays on screen 1 instead of opening an empty dashboard; Scout's outstanding question and its chips render on the landing (`.rsearch__reply`) since screen 1 now has to be answerable, and `hire-stage-flip.ts` FLIPs `.scout-composer` from the landing bar's last rect so one bar appears to travel. The suggestion stack auto-advances one line every 5s (was 3.5s) AND answers wheel/touch; the timer is keyed on the active index so a manual scroll restarts it, holds on hover/focus, and does not run under prefers-reduced-motion. Disabled Search keeps the teal skin. No action, ranking, repository or auth change.

- 2026-09-10 [rule] Scout search-first: after brief update, searchable (role OR skills OR track pool) auto-runs candidate search; stack/seniority/salary no longer gate first results; timeout/offline recovers via latest valid searchable brief + search fingerprint dedupe.
- 2026-09-10 [rule] T-149: markProjectOpenedAction no longer revalidates the request page (that recomputed isNew against now() and killed New badges). runMatchAction deleteMany is UNDECIDED-only so SHORTLISTED/REJECTED survive a rerun even when outside the latest top set.
- 2026-09-10 [schema|rule|convention] T-149 / TC-R-004 talent project persistence (plan 125): TalentRequest.name + lastViewedAt + TalentRequestMatch.viewedAt/decision writers in talent-project-actions.ts (updateMany scoped by recruiterUserId). Match-run upsert from T-044 is unchanged and still does not assign those state columns. Temporary desk UI for name / viewed / new / shortlist / reject; outreach cart relabelled "Add to request list". TalentList / RecruiterShortlistItem / T-027 untouched.
- 2026-09-10 [schema|env] T-259 error tracking: Sentry + pino structured logs; outbound mail audit is `OutboundDelivery` because T-248 already owns `NotificationDelivery`. `SENTRY_DSN` is production-only. Middleware stays edge-safe (request-id duplicated in middleware.ts, no `@/lib` import).
- 2026-09-11 [rule] Plan 127 admin access: `bootstrapAdminsFromEnv` (platform-wide, bailed once any active GLOBAL ADMIN row existed) is replaced by `bootstrapAdminFromEnv(userId)` in `lib/admin-auth.ts`, run only on a `hasPlatformAdmin` miss. An email added to `ADMIN_EMAILS` after the first admin now gets its `UserRoleAssignment`; a revoked admin still never does (the guard count includes revoked rows). This is why `/admin` and `/admin/jobs` 307'd to `/dashboard` while the header showed the env-derived Admin button.
- 2026-09-11 [rule|convention] Plan 127 recruiter approval removed: registering IS the access. `ensureRecruiterWorkspace(userId)` (features/hire/provision-recruiter.ts) provisions Organization + OrganizationMember + RECRUITER assignment + onboarding credits and heals legacy rows; `requireRecruiter`, `requireRecruiterWorkspace`, `recruiter-gate.ts`, `pool.ts` and `talent-project-actions.ts` no longer read `approved`. `RecruiterState` is `none | active`. Deleted: `/talent/pending`, `/talent/setup`, `recruiter-setup-form.tsx`, `recruiter-setup-actions.ts`, `admin-recruiter-actions.ts` (approve/reject). `/admin/recruiters` + `/admin/program/recruiters` are now a read-only directory; the admin nav pending badge and the overview "Recruiter applications" card are gone. `VerifiedRecruiterSeat` survives as a pre-verified company NAME only — `setRecruiterSeatActiveAction` no longer flips `RecruiterProfile.approved`. **No schema change**: `approved`/`approvedAt`/`setupStep`/`setupCompletedAt` stay, written true/COMPLETE/now and read as a gate nowhere. Trade-off: nothing holds or revokes recruiter access now beyond the work-email rule (T-225), which is unchanged.
- 2026-09-11 [convention] T-239 / TC-R-007 candidate review panel (plan 134): the `/hire` review panel is resizable — `PanelResizer` (components/hire/panel-resizer.tsx) drags a grip on the panel's left edge and writes `--hire-panel-w` to `:root`, persisted in `localStorage` under `abtalks-hire-panel-w`, clamped 360px..min(720px, 60vw) by `features/hire/review-panel.ts`. The variable is read by three CSS rules: both `.is-open` grid rules plus `.hire-app--results .scout__body`, the screen-2 column that PR #295 made always-present — if only the open state honoured the width the layout would jump on every open/close, and the card column changing size is what would break the scroll restore. The pre-results `1fr 0fr` state and the <=1100px `0fr 1fr` overlay stay literal, or a stored width holds a gap open behind the panel. The evidence resume now renders INSIDE the panel as a `data-section="resume"` section — `EvidenceResumeBody` split out of `evidence-resume.tsx` and shared with `/hire/evidence`, which is unchanged; the `•••` full-page link and the locked-preview `SubscriptionGate` both survive. Closing the panel restores the results scrollTop captured on the FIRST open (next/previous must not re-mark it), re-applied on `transitionend` guarded to `grid-template-columns`; `.chat-output` gained `scrollbar-gutter: stable` so the column is the same width open or closed. **UI only** — no schema, no new route, no new server action, no new dependency.
- 2026-09-11 [rule|convention] T-278 / TC-A-016 read-only admin talent project inspection (plan 135): new `/admin/hire/projects` list + `/admin/hire/projects/[requestId]` detail, both Server Components. **First admin carve-out to R1** (plan 115 §10 recruiter isolation), signed by Sohail on the conditions that it is a read only, creates no server action, and leaves every recruiter-scoped `recruiterUserId` predicate untouched — `hire/[requestId]/page.tsx` and `loadRequestMatches` are unchanged and still scoped, and a test pins both. `features/admin/inspect-talent-project.ts` resolves the OWNER's `recruiterUserId` first and then reuses `loadRequestMatches` byte-for-byte, so admin sees exactly the recruiter's view rather than a re-implemented unscoped query. Gated by the DB-checked `requireAdmin()` (R2 privileged-read clause); `middleware.ts` untouched — `/admin` is already in `protectedPaths`. The recruiter components are deliberately NOT reused: `ScoutChat` fires `markProjectOpenedAction`/`markMatchViewedAction` from mount effects (an admin visit would destroy the recruiter's New badges), `CandidateInspector` imports the billable `revealContactAction` with no disable prop, `GapReport` calls `requestCohortTrainAction`. Read-only is structural, not a disabled button: no file on the surface imports `@/app/actions/*` or calls a Prisma writer, and `test:admin-project-inspect` (11) enforces that plus a directory sweep. **Pipeline state = the `TalentRequestMatch.decision` buckets** — `PipelineStage`/`TalentListItem` are schema-only with zero code references. Searchability filter kept, so admin sees what the recruiter sees. No audit row: `AdminAction.targetUserId` is a required FK to `User` and cannot name a `TalentRequest` (plan 123 unsigned). **No schema change, no migration, no server action.** Logged out of scope: `placeEngagementRequestAction` accepts a foreign `requestId` unchecked (`hire-request-actions.ts:124`), `toggleShortlistAction` has a bare `auth()`, `unlockContactAction` is credit-spending and project-agnostic, `recordCandidateViewAction` has no auth gate at all.

_(Cleared 2026-08-24 — the 17 entries that were under this heading were folded into `docs/project-context.md`,
reconciled against `3b040d8`. The un-headed block above (2026-08-06 → 2026-08-20) was logged outside this
heading and was never reconciled; its facts were folded into the same pass, but the lines were left in place
rather than deleted without authorization. Cursor appends new lines below.)_
- 2026-09-10 [convention] `/hire` empty state is the forest-green Figma search landing (desktop 1570:437 + mobile 1594:98) until the first Scout query; Outfit heading, Inter UI, 55px frosted header, clay teal Search.
- 2026-09-10 [schema|rule] Plan 121 recruiter assessment builder: RecruiterAssessment + AssessmentQuestion + AssessmentQuestionOption (enums RecruiterAssessmentStatus, AssessmentQuestionType); routes /hire/create-test and /hire/assessments; workspace-scoped via requireRecruiterWorkspace (foreign id → NOT_FOUND); DRAFT-only full-replace question writes; no publish/assign/attempt.
- 2026-09-10 [convention] `/hire` desk after the first query is the Figma results screen (1585:46, `.hire-app--results`): #F4F4F5 ground, `HireSidebar` nav card replaces the `HireJourney` rail on desktop (phones keep the step strip), Filters button opens the Requirement menu, result card = meta row + skills + AI Summary (no score/evidence pills), `CandidateInspector` is a tabbed profile panel. Desk context gains `projectName` + `requestNewSearch`; `coverageLede` moved to hire-card-facts.
- 2026-09-10 [schema|rule] T-246 candidate jobs (TC-C-007/008): JobApplicationStatus enum added (APPLIED default, REVIEWING, ACCEPTED, REJECTED). JobApplication extended with status, resumeUrl, coverLetter, updatedAt; composite unique key reordered to @@unique([userId, jobId]) so single-apply is enforced by Postgres (the ticket's explicit rejection of UI-only guards). New src/features/candidate-jobs/{lifecycle,service,prisma-store}.ts is Prisma-injectable: browsePublishedJobs strictly filters status='PUBLISHED' with location/workMode/opportunityType/skills filters; applyToPublishedJob reuses T-245's assertApplyAllowed for the CLOSED guard, maps Prisma P2002 to a 409 CONFLICT with DUPLICATE_APPLICATION_MESSAGE ("You have already submitted an application for this position"); listMyApplications returns the caller's own applications joined with job title/company/status. src/app/actions/job-actions.ts rewired through the service and adds listMyApplicationsAction. Composite-key access in get-job-detail.ts renamed jobId_userId → userId_jobId. Acceptance suite npm run test:candidate-jobs (12 tests, in-memory store that throws P2002-shaped errors) passes; requires prisma db push before deploy for the new columns and constraint swap. See docs/plans/120-t246-candidate-jobs.md.
- 2026-09-10 [rule] Admin soft-delete user account via deletedAt/anonymizedAt scrub (plan 119)
- 2026-09-10 [schema|rule] T-245 fully aligned with T-226 and pushed to Neon: (1) all recruiter-job mutations go through requireRecruiterWorkspace() — no jobs escape until setupStep=COMPLETE; (2) Job.company stamped from workspace.company on create, dropped from update payload; (3) new workspace-scoped reads listRecruiterJobs / getRecruiterJob (foreign-row → NOT_FOUND, not FORBIDDEN) plus listMyJobsAction / getMyJobAction server actions — recruiter UI inherits per-user isolation by construction; (4) JobStore + prisma-store gain listByRecruiter — there is no unfiltered "list all jobs" on the recruiter path. Three new T-226 tests (same-domain independence, foreign-row NOT_FOUND, cross-recruiter draft leak) bring the suite to 16/16; test:recruiter-workspace still 17/17. prisma db push applied — status defaulted every existing row to PUBLISHED, no backfill needed.
- 2026-09-10 [schema|rule] T-245 recruiter job lifecycle (TC-R-015): Job model gains status (JobStatus enum DRAFT|PUBLISHED|CLOSED, default PUBLISHED so historical rows stay live), workMode (JobWorkMode enum, optional), skills (String[] default []), recruiterId (nullable FK to User), publishedAt, closedAt. Job.skills relation renamed to skillLinks (no consumers). Business logic in src/features/recruiter-jobs/{lifecycle,service,prisma-store}.ts is Prisma-injectable; server actions in src/app/actions/recruiter-job-actions.ts (create/update/publish/close/reopen) go through requireApprovedRecruiterAction and refuse non-owning recruiters with FORBIDDEN. Candidates hitting a DRAFT job by id get 404 (not 403) so drafts cannot be enumerated; apply on a CLOSED job returns "This position is closed and no longer accepting applications". Admin toggleJobOpenAction + createJobAction now keep status/isOpen/publishedAt/closedAt in sync. Acceptance suite npm run test:recruiter-jobs (13 tests, in-memory store) passes; requires prisma db push before deploy for the new columns.
- 2026-09-10 [convention|rule] T-253 GA4 event instrumentation (plan: docs/plans/117-t253-event-instrumentation.md) ships on top of T-252's loader, which is unchanged. New `src/lib/analytics/events.ts` (pure: the 7 event names, a per-event parameter ALLOWLIST of bounded value sets, `sanitizeParams`, `skillCountBucket`/`scoreBucket`, and `hasAnalyticsConsent` derived from T-252's `toGaConsent`) plus `src/lib/analytics/use-track.ts` (`useTrack()` — the ONLY `gtag('event')` call site in the repo; no-ops unless consent is read, grants analytics_storage, and `window.gtag` exists). Emitted once each from the confirmed-success branch: `recruiter_reg_submitted` (recruiter-register-form), `site_profile_updated` + new required `section` arg on `useSectionSave` (wizard sections; Accomplishments still reports `certifications`), `site_skill_added` (skills-section, only when the save added a new skillId), `site_job_applied` (apply-job-button), `site_test_completed` (quiz-form, `score_bucket` only — never the score), `recruiter_contact_unlocked` (engagement-decision, only on a persisted CONTACT_SHARED), `recruiter_candidate_viewed` (new `TrackCandidateView` ref-deduped beacon on /talent/members/[id]). NOT tracked, deliberately: candidate visibility toggles, recruiter Google login, company admin, team invitations — and "job alert sent" (`runHireAlertsCron` is a browserless cron; reaching GA would need the Measurement Protocol) and "profile-view notification sent" (no such feature exists — nothing records a profile view or notifies a candidate). No PII: every parameter is a bounded enum, so names/emails/phones/notes/answers/ids cannot pass. Tests: `test:analytics-events` (19), `test:analytics-instrumentation` (9, source-level guards on single-transport, one-emit-per-event, exclusions).
- 2026-09-09 [convention] T-283 Wave-1 architecture guardrails locked (recruiter isolation, server-side authorization, database as source of truth, idempotency) in docs/plans/115-t283-demo1-security.md with a per-developer sign-off.
- 2026-09-09 [schema|rule] Demo 1 security: Platform Admin is UserRoleAssignment (GLOBAL ADMIN; ADMIN_EMAILS is bootstrap-only); RateLimitEvent added for unlock/outreach/search/export; recruiter contact payloads go through hasContactAccess/loadProtectedContact; guest Scout in-memory rate Map removed.
- 2026-09-08 [schema|rule] Hackathon registrations are now event-scoped: ViCodathon 2 uses `eventId = "vicodathon-2-2026"`, while all existing teams, participants, and removal records migrate to `legacy`; participant and team-code uniqueness are per event so historic registrations never grant current-event access.
- 2026-09-10 [schema|rule|perf] T-228 credits foundation: `CreditTransaction` (append-only, unique `idempotencyKey`, `balanceBefore`/`balanceAfter`, `seq`) is the source of truth for recruiter credits and `CreditAccount` is a cached projection read only inside `src/repositories/credits.ts`; amounts are integer USD cents; the $200 starting grant and the (placeholder) contact-unlock price live in the new `PlatformConfig` table, read through `src/lib/platform-config.ts`, so changing them needs no deployment; the grant is issued once per workspace at creation in `provisionRecruiterIdentity` (never on a read path) with pre-T-228 workspaces covered by `npm run db:backfill:credit-grants -- --apply`; `completeRecruiterSetupAction` moved from the interactive `$transaction` to the batched array form (~7 Neon round trips to 3) via the new `recruiterIdentityWrites`. Credit movements must run with `{ maxWait: 20000, timeout: 20000 }` — Prisma's 5s default is too short once transactions queue on the account row lock, and T-229's unlock must pass it too. The seeded unlock price is a labelled PLACEHOLDER, not an approved product decision — T-229 must not treat it as final until product confirms it. `prisma/cleanup.ts` now clears credit rows before deleting users because the ledger is deliberately ON DELETE RESTRICT. No contact unlock — T-229/T-230 consume this.
- 2026-09-08 [convention] Hackathon landing redesigned to the new mock at /hackathon (light Student-Profile-shell + peach hero with animated padlock/circuit + Discover HIW + Discord callout + Timeline + FAQ + Rules). Ported from /Users/byteninjaa/Downloads/abtalks-hackathon/ into src/app/hackathon/_styles/hackathon-v2.css (font tokens rewired to project vars: --font-inter, --font-outfit, --font-hub-instrument-sans; Sora → Outfit 800) + src/components/hackathon-v2/{hackathon-shell,hero,countdown-v2,faq-accordion,registration-dialog-trigger}.tsx + 8 static assets under public/hackathon-v2/. /hackathon/dashboard and /hackathon/submission moved into /hackathon/(app)/ route group so their dark-theme HackathonHeader layout is unchanged; only /hackathon adopts the new light shell. MainShell no longer applies bg-black on /hackathon. Padlock renders data-state="locked" (green LEDs) by default and flips to "unlocked" (orange LEDs) once getMyRegistration returns non-null after a successful popup submit + router.refresh(). Kill switch HACKATHON.registrationOpen flipped to true; event dates rolled to Fri 25 Sep → Sun 27 Sep 2026 (kickoffUtc, deadlineUtc, registrationClosesUtc, rosterLockUtc + matching labels).
- 2026-09-08 [convention|rule] Hackathon /hackathon/register route DELETED; every referrer repointed to /hackathon (hackathon-auth-actions redirectTo, hackathon-header login-from, submission page redirect, challenge-switcher URL, invite-panel copy, success-panel copy, hackathon-email Register-again link, issue-hackathon-award-certificates error text, middleware protectedPaths). Registration is a Dialog wrapping the existing RegistrationForm (SOLO/TEAM_CREATE/TEAM_JOIN unchanged, new optional onSuccess prop calls router.refresh()). HACKATHON.discordLink (https://discord.gg/Zk8XRJ3J7) surfaced in DiscordSection + SuccessPanel + landing Discord callout + step 2 of Discover How It Works.
- 2026-09-08 [convention] CLAUDE.md "Your role here" changed from architect-only to plan-by-default + explicit implement-mode; memory architect-not-implementer updated to match.
- 2026-09-04 [rule] Admin Grant Synergy per-grant ceiling raised from 3000 to 4000 (server Zod + grant dialog)
- 2026-09-04 [rule] Submission synergy capped at a flat 10 SP per IST calendar day per user across ALL enrolments (plan 111): a user with 4 challenges could earn 4x per day. computeSubmissionSynergy and the GitHub (+5) / LinkedIn (+8) proof bonuses are deleted, so one submission is worth exactly SYNERGY_BASE_SUBMISSION; the marketplace earning pills that advertised those bonuses are removed too. The gate is the new flag-aware repositories/points.ts reader hasEarnedSubmissionPointsOnIstDate (SynergyEvent type=SUBMISSION while ENABLE_NEW_POINTS_WRITES is off, PointsTransaction ACTIVITY_ATTEMPT once on, positive amounts only), read under a lockWalletBalance row lock so concurrent submissions in different challenges cannot both grant. First submission of the IST day wins; later ones award 0 and write no ledger row (dual-write records pointsAwarded 0). Referral and admin grants stay UNCAPPED. Forward-only: no historical grant recomputed or clawed back.
- 2026-09-04 [schema|rule|convention] Data Solutions Architect cohort (plan 110) ships 078-native at /program/ds-architect: LearningProgram "ds-architect" + ROLLING open Cohort "ds-architect-open", ProgramEnrollment/ActivityAttempt/ActivityEvaluation/EnrollmentProgress/EnrollmentDayActivity with PLAIN CUID ids (never pe_*/aa_* prefixes), no dual-write and no legacy ProgramMember/ProgramDay row. Content from prisma/content/Data-Sol-Architect only (modules + days.missionSpec + videos). Day 1 = enrolment date in IST, 10 days, 175 pts, 30 runs/day, 15s spacing. Missions reuse verify-mission.ts unchanged. Gated by ENABLE_DS_ARCHITECT.
- 2026-09-04 [schema|rule|convention] Power BI & Analytics cohort (plan 111) ships 078-native at /program/powerbi, a third instance of the Databricks/DS-Architect open-cohort shape (deliberately copied, NOT abstracted): LearningProgram "powerbi" + ROLLING open Cohort "powerbi-open", ProgramEnrollment/ActivityAttempt/ActivityEvaluation/EnrollmentProgress/EnrollmentDayActivity with PLAIN CUID ids, no dual-write and no legacy ProgramMember/ProgramDay row. Content from prisma/content/powerbi-cohort only (modules + days.missionSpec + videos); activity ids are act_pbi_day_NN / act_pbi_vid_NN_NN. Day 1 = enrolment date in IST, 7 days, 6 modules, 125 pts, 30 runs/day, 15s spacing. Day 1 is DATA_ROOM (answers only — its repoChecks are stored but never run, and shipItHints stay hidden there); days 2-7 are SHIP_IT (answers + repo file existence). videos.json ships youtubeId: null for every row — they seed but the day-shell reader skips them until curated. Missions reuse verify-mission.ts unchanged. Hub heatmap now also counts act_pbi_day_* attempts. Prep Kit grid went from 3 to 4 cards and wraps by breakpoint (1/2/3/4 at base/sm/lg/xl) rather than scrolling horizontally. Gated by ENABLE_POWERBI.
- 2026-09-03 [rule] /profile Career Preferences completes on any saved engagement (incl. openToWork); profile card shows orange Open To Work tag when set
- 2026-09-03 [rule] /profile Skills section completes with ≥1 claimed skill (was MIN_SKILLS = 3)
- 2026-09-03 [rule] /profile Basic Information requires phone OTP when verification is on (India); verified state shows green badge only
- 2026-09-03 [rule] /profile Other skill path find-or-creates Skill rows (slug fold) so custom names stay recruiter-searchable
- 2026-09-03 [env] Profile wizard avatar upload uses a new public Vercel Blob store (`avatar_READ_WRITE_TOKEN`, `avatar_STORE_ID`); without them the pencil is hidden and `/profile` is otherwise unchanged.
- 2026-09-02 [schema] **New table — run `prisma migrate deploy` before this branch serves traffic.** Plan 106 adds `CandidateResume` (one row per candidate, unique on `userId`, cascade delete) plus enums `ResumeSourceType` and `ResumeProcessingStatus`, in migrations `20260902120000_candidate_resume`, `20260902160000_candidate_resume_enrichment` (`documentVersion`, `appliedAt`, `appliedSections`) and `20260902190000_candidate_resume_merge_log` (`mergeLog`). Purely additive — no existing column is altered or dropped. Vercel applies it automatically via `build:deploy`; any OTHER database (a teammate's local, a Neon child) needs `npx prisma migrate deploy` run against it, or the /profile résumé section degrades to its empty state and every upload fails. Also requires a new env var `BLOB_READ_WRITE_TOKEN` (Vercel Blob, private access) for storing the original file; without it parsing and scoring still work but the file is not retained. `GEMINI_API_KEY` (already set) drives the parser. `CandidateProfile.resumeUrl` / `StudentProfile.resumeUrl` are unchanged and still mirrored. NO other new tables: the résumé is MERGED into the EXISTING `CandidateEducation` / `CandidateExperience` / `CandidateProjectEntry` / `CandidateSkill` / `CandidateCertification` rows by the merge service at `features/resume/merge/` (semantic term identity, entity matching on several signals, bullet de-duplication) applied through `repositories/candidate-merge.ts`. Additive by construction: the plan can only create a row, append to a list, or fill a field that is empty — there is no operation for deleting, replacing or editing candidate-entered data, and re-uploading the same résumé is a no-op.
- 2026-09-01 [convention] design-system layout-reveal: docked-field height uses --dur-3 / --ease-spark (grid 0fr/1fr); first use is the /hire Scout composer strip.
- 2026-09-01 [rule] `/` now resolves signed-in users to their working surface: any database-backed recruiter profile, pending or approved, redirects to Scout at `/hire`; all other signed-in accounts redirect to `/dashboard`. Signed-out visitors still receive the marketing landing page.
- 2026-09-01 [convention] Landing page ported the abtalksv16globe reference deltas: NEW Global Community section (`components/landing/site/global-community-section.tsx` + `-data.tsx`, rendered between FAQ and Community) — a dotted equirectangular world map with a self-building 8-node network, all driven by one config (GC_LOCATIONS/GC_ROUTE/GC_T); land dots are a base64 144x56 bitmap projected to three SVG depth bands at module load, so the map server-renders (+13 KB gzipped) and  pins/cards/routes are positioned imperatively via refs on mount/resize, React state holding only the two on/lit counts. `KeepThreeSection` now takes `getStartedHref` and shows a `.keep__actions` CTA under the pipeline; its label changed to "The platform and the process". Hero video replaced with the new 1280x720 interview clip — hero.mp4 (2.2 MB, H.264+AAC, faststart, still `muted`), hero.webm (876 KB VP9/Opus) and hero-mobile.mp4 (346 KB, 406x720 centre crop) ALL regenerated from it, since the webm `<source>` is listed first and a stale one would keep serving the old video. No schema change.
- 2026-08-26 [convention] Claude Challenge (/claude + /claude/day/[day]) restyled onto hub DashboardShell (Claude-only desktop collapse) and cream/orange AI Cohort language; heatmap/stats/AppHeader removed from Claude UI; no schema change
- 2026-08-25 [convention] Campus Ambassador onboarding banner and derived notification-bell item removed from student surfaces.
- 2026-08-25 [convention] Campus Ambassador onboarding banner unmounted from dashboard hub and challenge track pages.
- 2026-08-25 [convention] AUTH_SECRET + session cookie v2 to stop JWTSessionError; login/register use scoped orange outline CTAs.
- 2026-08-25 [convention] Promo Claude/hackathon overlays unmounted; cookie/terms consent and AI Cohort landing use scoped orange; apply pages stay purple.

- 2026-08-24 [schema|rule|convention] Plan 082 on feat/hire-scout-078 lands PR #200 (/hire Scout) on the 078 architecture: CandidateAvailability dropped for CandidatePreference (repositories/candidate.ts owns the mapping and ensures a CandidateProfile row first); TalentRequestMatch/TalentEngagementRequest keyed on candidateUserId with programMemberId demoted to un-FK'd provenance; searchableUserWhere() in repositories/talent.ts is the single recruiter-discovery gate, ANDed onto every candidate query inside the new src/repositories/hire.ts seam so a caller cannot omit it, and now covering challenge and hackathon candidates (which had none) plus a re-filter of stored TalentRequestMatch rows on read; candidate-declared salary never reaches a recruiter surface; recruiter registration and admin approval also provision UserRoleAssignment(RECRUITER)+Organization+OrganizationMember with VerifiedRecruiterSeat narrowed to a pre-account invite; track registry carries the 078 cohort slug; hire plan docs renumbered 083-094. ENABLE_NEW_* stay off and legacy stays authoritative; the 078 read branch is additionally gated on NEW_HIRE_POOL_READY until Phase 2e/2f complete
- 2026-08-25 [env|convention] Blurred "Pro" preview cards on an empty /hire search, behind HIRE_PRO_PREVIEW (default off, read on the server and passed to the desk as a prop). features/hire/locked-preview.ts fabricates 3 deterministic example profiles from the recruiter's own spec — name/location/education/email/phone/compensation are generated, never anonymised real candidates — and components/hire/locked-field.tsx blurs them with an "Upgrade to Pro to see the full profile" prompt. Refs keep the SAMPLE: prefix so the whitelist still refuses them: a preview can never be shortlisted or become an engagement request. Pro is presentational only (no plan column, no entitlement, no billing) and is a COMMERCIAL gate that must never widen the privacy gate — contact is still released only by an admin-approved TalentEngagementRequest reaching CONTACT_SHARED, and paying cannot skip that
- 2026-08-25 [env|rule] Recruiter login/register closed unless ENABLE_RECRUITER_AUTH=true: /talent/login and /talent/register show a closed page, hire Sign in/Register buttons are hidden, and recruiter-otp authorize plus OTP/register actions refuse new sign-ins; /hire guest browsing stays public
- 2026-08-25 [rule] Program members start at Day 1 (PROGRAM_MEMBER_START_DAY); Days 1–3 are no longer waived at enroll
- 2026-08-25 [rule] Phase 5 pass #2 waits until 2026-08-25T15:57:57Z; extras treat recruiter-visible as all ProgramMembers plus platform_default (not consent-only); ENABLE_NEW_* stay off until that pack is clean
- 2026-08-25 [convention] 078 points count-drift ignores Phase 2f User.synergyPoints plug rows (idempotencyKey reconciliation:phase2:*); those txs have no SynergyEvent by design and must not be deleted
- 2026-08-25 [rule] Dual-write now covers CandidateProfile on registration/profile update and Credential on certificate issuance (legacy still authoritative; ENABLE_NEW_* stay off)
- 2026-08-26 [rule] Phase 5 24h wait elapsed; 885d37a continuous-write is live; accelerated final gate is unscoped pass #2 now (no extra 24h after this deploy); ENABLE_NEW_* stay off; do not start Phase 6 until that pack is clean
- 2026-08-26 [rule] Phase 5 complete: second unscoped pack clean after 24h dual-write; ENABLE_NEW_* still off until Phase 6 switches
- 2026-08-26 [env] ENABLE_NEW_CREDENTIAL=true on production after clean smoke + recon
- 2026-08-26 [convention] Phase 6 CREDENTIAL complete: /verify /achievements /download read via credential repository; ENABLE_NEW_POINTS is next; dual-write and legacy Certificate writes stay
- 2026-08-26 [env] ENABLE_NEW_POINTS=true on production after clean smoke + recon
- 2026-08-26 [convention] Phase 6 POINTS complete: header chip /marketplace /admin SP display read via points repository; ENABLE_NEW_CANDIDATE is next; dual-write and legacy User.synergyPoints writes stay
- 2026-08-26 [env] ENABLE_NEW_CANDIDATE rolled back to false on production: CandidateProfile.referralCode differed from StudentProfile for 6 users (including a post-launch registration); profile display vs registration lookup would split-brain
- 2026-08-27 [env] ENABLE_NEW_CANDIDATE=true on production after referral-owner contract + clean smoke + recon
- 2026-08-27 [convention] Phase 6 CANDIDATE complete: live identity reads require CandidateProfile then return StudentProfile view; referral lookup stays on StudentProfile; ENABLE_NEW_LEARNING is next; dual-write and legacy StudentProfile writes stay
- 2026-08-27 [rule] ENABLE_NEW_CANDIDATE reads CandidateProfile + profile-owned edu_sp_/exp_sp_ + CandidateSkill without StudentProfile overlay; referral lookup follows the flag; dual-write copies the live SP referral code and only submitted identity fields; LEARNING stays off
- 2026-08-27 [convention] Phase 6 CANDIDATE genuine reads complete after referral repair + skills/edu/exp catch-up; ENABLE_NEW_LEARNING stays off; admin list/CSV/campus-ambassador remain StudentProfile until Phase 7
- 2026-08-27 [rule] Phase 6 LEARNING read-switch is in the repo behind ENABLE_NEW_LEARNING=false: student catalog/enrollment/membership/quiz definitions go through repositories/learning.ts; completion/progress stay legacy; StudentProfile.domain is pre-StudentProfile-retirement debt and is unchanged
- 2026-08-27 [convention] Program dashboard continue card previews the next locked day with an unlock date; VIEW STATS shows cleared/31 progress instead of a second cohort-day /31
- 2026-08-27 [schema] ContentActivityConfig.missionType stores the exact ProgramDay.missionType (CODE_SPRINT/DATA_ROOM kept distinct); LEARNING stays off
- 2026-08-27 [env] ENABLE_NEW_LEARNING=true on production after clean smoke + recon
- 2026-08-27 [convention] Phase 6 LEARNING complete: student catalog/enrollment/membership/quiz definitions read via learning repository; ENABLE_NEW_PROGRESS is next; dual-write and legacy writes stay
- 2026-08-27 [convention] Phase 6 PROGRESS pre-flip audit: prisma/scripts/compare-078-progress.ts is the read-only production pack; ENABLE_NEW_PROGRESS stays false
- 2026-08-27 [rule] Phase 6 PROGRESS dual-write closed (quiz attempts, admin delete, waivers, unlock/skip, commits, resubmit lateness); student progress reads are flag-gated from ActivityAttempt+Evaluation, not EnrollmentProgress; ENABLE_NEW_PROGRESS stays false
- 2026-08-27 [rule] Phase 6 track streak keeps Enrollment.currentStreak / longestStreak snapshot; live recompute is a separate Phase 7 product decision; ENABLE_NEW_PROGRESS stays false
- 2026-08-27 [env] ENABLE_NEW_PROGRESS=true on production after clean smoke + recon
- 2026-08-27 [convention] Phase 6 PROGRESS complete: student progress reads via ActivityAttempt+ActivityEvaluation (not EnrollmentProgress); Enrollment streak snapshots preserved; ENABLE_NEW_TALENT is next; dual-write and legacy writes stay
- 2026-08-27 [convention] Phase 6 TALENT pre-flip pack: prisma/scripts/compare-078-talent.ts; ENABLE_NEW_TALENT stays false
- 2026-08-27 [rule] dualWriteProgramMember opens a pre-existing non-withdrawn CandidateVisibility row so ProgramMembers are recruiter-searchable; challenge dual-write still does not flip a closed historical row
- 2026-08-27 [convention] Talent dark switch: /hire and /talent read CandidateVisibility+Profile+edu/exp/skill/preference when ENABLE_NEW_TALENT=true; NEW_HIRE_POOL_READY removed; openToWork is an explicit recruiter filter not the discovery gate; flag stays false
- 2026-08-27 [env] ENABLE_NEW_TALENT=true on production after clean flag-ON rehearsal + deploy (dpl_BZZ1uXwHzbKagt7WErbCyH7wNuqV)
- 2026-08-27 [convention] Phase 6 TALENT complete: /hire and /talent read CandidateVisibility+Profile+normalized children; Phase 6 complete; dual-write and legacy writes stay; do not start Phase 7
- 2026-08-27 [convention] Phase 7 shutdown matrix: docs/plans/099-phase7-shutdown-matrix.md; dual-write stays; no legacy writes stopped; first write-family candidate is Points after redeem/admin guards move to PointsAccount
- 2026-08-28 [env|convention] W1-A Points write authority: ENABLE_NEW_POINTS_WRITES (default off); applyPointsChange makes PointsAccount+PointsTransaction authoritative when on; User.synergyPoints and SynergyEvent stay mirrors; dual-write stays on; flag not enabled
- 2026-08-28 [convention] W1-A legacy points mirrors flush in a separate transaction after PointsAccount+PointsTransaction commit; POINTS_FAIL_LEGACY_MIRROR is child-only
- 2026-08-28 [env] ENABLE_NEW_POINTS_WRITES=true on production after child rehearsal, redeploy (dpl_CHEu4HMQTQ2Gp9pN7axKm2YcfBZY), and controlled +1 grant; User/SynergyEvent remain mirrors; dual-write stays
- 2026-08-28 [convention] MIGRATION PAUSED AFTER PHASE 6 + W1-A: Phase 1–6 complete; all new read flags live; PointsAccount+PointsTransaction authoritative for Points writes; User/SP/SynergyEvent mirrors remain; ENABLE_DUAL_WRITE on; W1-B and W2–W8 not started; no legacy tables dropped; resume at Phase 7 W1-B mirror shutdown prep
- 2026-08-28 [schema] Merged PR #197 interview agent 2.0 (GeneralInterview/InterviewTurn/InterviewReport); migrations 20260813000000 and 20260820000000 are in the repo but not applied to production
- 2026-08-28 [convention] W1-B prep only: User.synergyPoints/SynergyEvent read audit plus PA→User/SP mirror repair script; mirrors still written; W1-B write-stop not started
- 2026-08-28 [rule|convention] TALENT leftovers: searchableUserWhere is the hire/talent discovery gate; openToWork is an explicit recruiter filter; ProgramMember dual-write opens CandidateVisibility (challenge dual-write does not reopen a closed historical row)
- 2026-08-31 [convention] Student program app moved from /program to /program/ai-cohort; bare /program redirects to /dashboard (temporary); legacy apply/dashboard/day URLs 308 to the new prefix
- 2026-08-31 [schema|env|convention] Local-only /program/databricks module catalog on 078 LearningProgram (ENABLE_DATABRICKS; no legacy ProgramModule writes)
- 2026-08-31 [schema|rule|convention] Databricks cohort (plan 104) ships 078-native at /program/databricks: LearningProgram "databricks" + ROLLING open Cohort "databricks-open", ProgramEnrollment/ActivityAttempt/ActivityEvaluation/EnrollmentProgress/EnrollmentDayActivity with PLAIN CUID ids (never pe_*/aa_* migration prefixes), no dual-write and no legacy ProgramMember/ProgramDay row. Day 1 = enrolment date in IST, day N unlocks anchor+(N-1) and still needs day N-1 passed; late passes are marked per day and a late day 31 marks overall completion late. Missions reuse features/program/verify-mission.ts unchanged (SHIP_IT existence-only checks + DATA_ROOM answers), 12 pts/day, 372 max, 30 runs/day, 15s spacing. No join code, capacity, admin panel, entry exam, videos, leaderboard, concepts, exercises, projects or certificates. Gated by ENABLE_DATABRICKS, now the launch switch rather than local-only.
- 2026-08-31 [rule] Databricks day pages now render Reference Resources from 078 Activity VIDEO rows (`act_dbx_vid_*`, seeded from prisma/content/Databricks/videos.json); still no ProgramVideo / Videos tab / leaderboard.
- 2026-08-31 [schema|rule|convention] Detailed candidate profile Slice 1: /profile rebuilt as a nine-section collapsible editor (Basic information, Experience, Education, Projects, Skills, Certifications, Links, Career preferences, Evidence & achievements) writing the 078 candidate tables directly — repositories/candidate-detail.ts owns the reads/writes and repositories/candidate-primary.ts holds the shared pure rules. Three invariants were fixed on the way. (1) syncCandidateSkillsFromLegacy is now ADDITIVE ONLY: it used to delete any CandidateSkill missing from StudentProfile.skills when it carried no evidence, which would have destroyed structured claims every time a legacy form saved past its 10-item cap. CandidateSkill is authoritative; only saveSkillClaims removes a claim, and a claim on a skill that has SkillEvidence is withdrawn via claimedByCandidate=false rather than deleted, because SkillEvidence cascades off the row. (2) repositories/candidate.ts no longer filters education/experience to the deterministic edu_sp_/exp_sp_ Phase 2 rows: the single value legacy surfaces need is chosen by pickPrimaryEducation/pickPrimaryExperience (isCurrent → most recent end → sortOrder), yearsExperience is now the OVERLAP-MERGED span across all roles rather than one row's duration, and syncProfileOwnedEducation/Experience no-op once the candidate owns real rows so a legacy form cannot overwrite structured history. A section save is a full replace, which is what finally clears the edu_sp_/exp_sp_ artifacts. (3) Legacy mirroring runs canonical → StudentProfile only, per-field, and clears rather than going stale when a section is emptied; no save path reads StudentProfile as a source, touches domain, or rewrites referralCode. Migration 20260831120000_candidate_profile_detail is additive/nullable/zero-downtime: CandidateEducation.startMonth/endMonth/gradeType(GradeType enum)/description, CandidateProjectEntry.techStack, CandidateSkill.claimedByCandidate (DEFAULT true), CandidatePreference.opportunityTypes (OpportunityType enum array), and the new CandidateLink table (CandidateLinkType) — NOT YET APPLIED, and .env points at production. Profile strength is UX-only: weights total 125 and cap at 100 so a student with no employment and no certifications still reaches 100%, it takes no CandidateVisibility or openToWork input, and it gates nothing. Skills use the canonical Skill catalog via /api/skills/search + getPopularSkills (no new package, no create-on-type); selfRated stays strictly separate from verified/evidenceScore. Evidence & achievements is read-only from SkillEvidence/Credential/CandidateAchievement with nothing inferred. GitHub accepts a handle or any profile URL and stores the handle. Degrees/departments/roles are static constants in lib/candidate-vocab.ts with free entry, company stays free text, Organization is untouched. /register is unchanged. ENABLE_* flags and dual-write untouched; legacy tables intact. DEBT: RecruiterReview still stores education/experience/projects/certifications as JSON and is a competing candidate-facts store — canonical direction is the 078 tables, not yet migrated.
- 2026-09-07 [rule|convention] "Open to work" is now visible to recruiters (plan 113). The candidate toggle in /profile already wrote CandidatePreference.openToWork and the flag already reached AvailabilitySnapshot, the dossier and the scorer — but toPublicMatch never put it on MatchCardData, so no recruiter surface could draw it. openToWork now rides ScoredCandidate (derived in scoreCandidate from member.availability, beside availabilityUnknown) and MatchCardData (REQUIRED, so every card mapper must decide), rendered by one shared OpenToWorkBadge in components/hire/hire-card-facts.tsx on the Scout desk card, the inspector, the evidence resume, MatchCard, Saved for Later, the Shortlist pod, the cart and /talent/members/[id]. Deliberately NOT a pill in buildCardPills — every consumer of that row already prints a name, so it would duplicate and spend one of the 4-5 capped slots. Saved requirement pages (loadRequestMatches) read it LIVE via loadAvailabilityByUserId rather than from the frozen TalentRequestMatch.evidence blob, same rule as the recomputed compensation band, so switching the toggle off clears the badge everywhere; nothing new is persisted into that blob. hackathon-dossier.ts stopped hard-coding availability: null and now loads it, which is why a hackathon candidate could never have shown the badge (side effect: a hackathon candidate with a preference row no longer reads "Availability unconfirmed"). ShortlistRow/TalentProfile (features/talent-pool/pool.ts) gained openToWork from listCandidateAvailability; GuestCartItem/DeskShortlistItem/CartRow carry it optionally so a localStorage list written before this change still parses. NOT a discovery gate: searchableUserWhere() is untouched and openToWork stays absent from it and from memberEligibilityWhere (both asserted in visibility.test.ts), searchable-and-not-looking still ranks normally, and the recruiter's explicit openToWork filter is unchanged. Declared salary still never leaves toPublicMatch — a new visibility.test.ts suite source-scans that file for "expectedSalary". No schema change, no migration, no flag.
- 2026-09-07 [convention] Plan 115: `notifyUser` contract frozen as a typed no-op stub (src/features/notification/notify.ts) so other work can be written against it before the implementation lands. Nothing is persisted and no email is sent — both channels return "skipped"/"not_implemented", notificationId is null, and the call never throws or returns ok:false. Public names in src/features/notification/types.ts are now frozen: NotificationEventKey (submission.approved | submission.rejected | certificate.issued | recruiter.approved | admin.direct — a TS union deliberately stored later as a String column, NOT a Prisma enum, so new values need no migration), NotifyChannel, NotifyDelivery, NotifySkipReason (not_implemented is permanent, removing a union member would break callers' exhaustive switches), NotifyInput, NotifyResult, NotifyResponse (named that, not Result, because every action file already declares a local Result<T> that would collide on import) and PERSONAL_KEY_PREFIX "user:" extending the existing AppNotification.key namespace. Reuses rather than duplicates: NotificationCategoryKey verbatim for the category field (still presentation-only), and sendEmail/SendEmailResult from @/lib/email via the one real function here, emailDeliveryFrom, which maps {ok:true}→delivered, {ok:false,skipped:true}→skipped, {ok:false}→failed — that type-only import is what makes the email reuse compiler-enforced instead of a comment. AppNotification gained an optional isPersonal, reserved and always undefined until the read path lands. types.ts must stay client-safe (notification-provider.tsx imports it); notify.test.ts enforces that with a source scan for server-only/@prisma/client/@/lib/ alongside the mapping and shape checks. No schema change, no migration, no DB access. Implementation remains docs/plans/114-unified-notify-helper.md.
- 2026-09-09 [convention] /profile redesigned to the report-card layout. The workspace is a Quick Links card (section tabs, no identity block, no performance panel — the two recruiter-activity figures there were placeholders nothing measured, and the card now hugs its content and scrolls internally) plus a scrollable report card, "your profile as a recruiter sees it"; every section form moved into a right-hand slide-over (`.pw-form-sheet`). New `src/features/profile/build-review.ts` builds that report card SERVER-SIDE from the same `CandidateDetail` the wizard edits (plus `getResumeView` + mock-interview count) into a plain `ProfileReview` of typed blocks — no functions/icons cross the RSC boundary, and each card carries the wizard step index its Add/Edit opens, derived from `steps` rather than restated. New client components `profile-review.tsx` (report card) and `identity-media.tsx` (clay ring + avatar + photo pencil + completion tick, which now live in the report-card hero). The sheet is ALWAYS editable — there is no locked/read-only state and no edit pencil, so switching sections via Quick Links never lands you on a form you cannot type in — and its action row is exactly two adjacent trailing buttons, Cancel (bordered) and the highlighted Save ("Done" on Mock Interview and Résumé, which persist their own changes); Previous/Next are gone. Section headings lost their step number and are bold. The sheet stack was deliberately placed at z-44/45/46 — above DashboardHeader (z-40), BELOW the shadcn Dialog layer (z-50) — because at z-190 it hid the "Save your changes?" modal it raises; LeaveDialog is pinned to z-60 as well. Unsaved-edit guarding still runs on Esc, scrim, × and section switches (Cancel discards outright, by design). profile-wizard.css re-ported to the new reference: orange #E55636, 15px card radius, #FBF9F7 ground, #FFFFFF field fill on #E8DFD9 borders, 48px fields, plus the rv-* report-card scale; `pw-menu-select-*` restyled to the reference's pick-menu rather than a second dropdown system being introduced. One deliberately global rule, `body.pw-sheet-open{overflow:hidden}`, scroll-locks the page behind an open sheet (the wizard reserves the scrollbar width so nothing jolts). /profile keeps the standard DashboardFooter (same as /dashboard) and passes `contentClassName="flex flex-col"` so the content pane is a flex column and `.pw-root` flex-fills the exact gap between header and footer — no hard-coded chrome offset, so the footer stays flush at any footer height. `.pw-root` is itself the scroll container (`overflow-y:auto`, scrollbar hidden): the report card grows with its content and scrolls there while the Quick Links card is `position:sticky; top` and holds until the report card is scrolled through, then releases with the page (standard sticky). No card scrolls internally any more — the earlier `height:100%` + inner-`overflow` model could leave nothing scrollable at all. `.pw-root.pw-sheet-open{overflow:hidden}` freezes that scroll while the sheet is open; the sidebar drops to `position:static` under 1024px. /profile also passes `collapsible`, so the ABTalks sidebar folds to 72px and the workspace reflows on the sidebar's existing width transition. Server-computed completeness, the Server Actions, the Zod schemas and the repository write paths are untouched; `public/new-profile-reference/` deleted. Last-updated survives as a hero meta chip (formatted on the server); the percentage badge under the ring is gone, replaced by the hero's "Profile strength" bar.
- 2026-09-09 [ui] `/hackathon` landing now renders the shared ABTalks `DashboardSidebar` instead of a bespoke copy in `HackathonShell` — this removes the `document.body.overflow = "hidden"` layout shift on drawer open, gains real active-route/sign-out/avatar behaviour, and drops the duplicated `.ab-sidebar*`/`.ab-nav*`/`.ab-scrim` CSS; the dead `.ab-chat` hero FAB is gone, the hackathon prize-pool FAQ answer reads ₹30,000, and the WhatsApp group invite moved to a single source (`HACKATHON.whatsappLink`, now consumed by `lib/hackathon-email.ts`). `DashboardSidebar` gained an optional `signedIn` prop (default true, so every gated route is unchanged) because `/hackathon` is public and guests were being shown a Sign out button and a "User" tile; they now get a Log in link.
- 2026-09-09 [schema|rule|convention] Profile Accomplishments section replaces Certifications in Quick Links (step key `certifications` → `accomplishments`; the completeness scoring key stays `certifications`, since what the tab is called is not what it is worth). Four parts in ONE form with one Save: (A) Verified Accomplishments, DERIVED and read-only — new `src/features/profile/get-verified-accomplishments.ts` reads Credential (via the flag-aware `repositories/credentials.listForUser`, never a raw Certificate/Credential table read), Enrollment, ProgramEnrollment and HackathonParticipant. Claude shows only once its certificate exists; SE/AI/DS show at `daysCompleted >= 50` read through `getChallengeProgressStats` so the 078 progress flag is honoured; Cohort shows on ProgramEnrollment COMPLETED/completedAt; ViCoDathon shows the best of Winner / Runner Up / Second Runner Up / Top 5 from the certificate metadata's `hackathonVariant`, else "Participated in ViCoDathon" when the team has a submission. Profile-only placement wording lives in HACKATHON_PLACEMENT_LABELS — HACKATHON_VARIANT_LABELS keeps saying what is printed on the PDF for /achievements. Crucially the profile read has NO write side effect: unlike getAchievements it never calls ensureClaudeCertificate, so opening /profile cannot mint a certificate. There is no write path for verified rows at all, asserted by two new suites in profile.test.ts (39 passing). (B) Certifications keep the existing fields and CandidateCertification replace-wholesale write. (C) Awards is ONE prose field, new column `CandidateProfile.awards TEXT NULL` (migration 20260909120000_candidate_awards, additive/nullable/zero-downtime, NOT YET APPLIED). Deliberately not stored on CandidateAchievement: that table is the platform-evidence surface read by get-evidence.ts, and self-authored prose there would render as ABTalks-verified. (D) "View Your Certificates" links to /achievements from the Verified header. New `accomplishmentsSchema` + `saveAccomplishmentsAction` + `saveAccomplishments` (certifications and awards in one transaction; `awards: undefined` leaves prose untouched). The superseded chain was DELETED rather than left as a duplicate editor: components/profile/certifications-section.tsx, saveCertificationsAction, repositories saveCertifications, and certificationSectionSchema. Report card gains one Accomplishments card (verified → certifications → awards) in place of the Certifications card. No new model, no data duplicated.
- 2026-09-09 [rule|convention] Profile Skills split into candidate claims and ABTalks Verified Skills, and self-rating REMOVED end-to-end (UI select, `PROFICIENCY_LABELS`, `skillClaimSchema.selfRated`, `SkillClaimWrite`/`SkillClaimView.selfRated`, the `saveSkillClaims` upsert and its withdrawal clear). The `CandidateSkill.selfRated` COLUMN is deliberately left in place — dropping it is a destructive migration for a field nothing now writes; it is simply dead. Claimed skills render as compact pills (`.pw-skill-chip`, sized to match `.pw-quick-chip`) instead of tall rows, and the two explanatory paragraphs were deleted. NO NEW MODEL: verified skills use the EXISTING `ProgramSkill` (LearningProgram → Skill), which shipped with 078 and had never had a reader or writer. That one join table covers challenges AND cohorts because every track already owns a LearningProgram — the four 60-day challenges and the AI cohort got theirs from migrate-2d-learning-content.ts (`claude-challenge`, `software-engineering-challenge`, `data-science-challenge`, `ai-engineering-challenge`, `ai-cohort-program`), the newer cohorts from their own seeds (`databricks`, `powerbi`, `ds-architect`). New `prisma/content/curriculum-skills.json` + `npm run db:seed:curriculum-skills` fill it; the lists were read off the real content in prisma/content/, are core-only, and the seed folds onto existing catalog rows by slug then name/alias before creating any Skill, upserts links and removes links it no longer lists. New `src/features/profile/get-verified-skills.ts` derives at READ time — nothing stored per user, so adding a cohort's skills reaches everyone already past the bar with no backfill and the user has no write path. Bar is completion, never enrolment: challenges need `daysCompleted >= 50` via `getChallengeProgressStats` (flag-aware), cohorts need a COMPLETED `ProgramEnrollment`. IMPORTANT: challenge enrolments are mirrored into ProgramEnrollment as `legacy-<domain>` cohorts by dual-write.ts, so both this and get-verified-accomplishments.ts now exclude `CHALLENGE_MIRROR_COHORT_SLUGS` — that ALSO fixes a bug shipped earlier the same day, where a finished challenge was listed a second time as a cohort. `legacy-program-<id>` is not excluded; it is the real AI Cohort Program. Report card's Skills card gained a "Verified by ABTalks" block. profile.test.ts is now 43 passing, including suites asserting self-rating is gone from UI/schema/repo/vocab, that the derivation never writes, that the 50-day and COMPLETED bars are explicit, and that no curriculum skill name is hardcoded in the component. No schema change, no migration.
- 2026-09-09 [rule] T-225: a free consumer mailbox can no longer become a recruiter account. New `src/lib/validations/work-email.ts` owns one hardcoded `PERSONAL_EMAIL_DOMAINS` set (the four T-225 names plus provider aliases, Yahoo/Microsoft India variants, Apple, Proton, AOL/GMX/Mail.com and Rediff), one `WORK_EMAIL_REQUIRED_MESSAGE`, `isPersonalEmailDomain()` and a `workEmailSchema` — deliberately no env var, no table and no admin override, because an allowlist of any shape is the exception mechanism T-225 exists to remove. Enforced at all six server-side points that create or authorise a recruiter: `registerRecruiterSchema.email` (the Zod boundary), `requestRecruiterOtpAction` on `intent: "register"` only (refuses before a code is emailed; sign-in is untouched so pre-T-225 accounts are not locked out), `registerRecruiterWithOtpAction` again on the normalised address before `recruiterProfile.create`, `registerRecruiter` in features/talent-pool BEFORE the `VerifiedRecruiterSeat` lookup (so a seat can never pre-empt the rule — this is the Google-authenticated path), `addRecruiterSeatAction` via `workEmailSchema` (an admin cannot pre-verify a personal domain), and `approveRecruiterAction` before `recruiterProfile.update` (an admin cannot approve a legacy or hand-written personal-domain application). Candidate auth is untouched: `auth.ts`/`auth.config.ts` import nothing from this module and Google still creates a `User` only, never a `RecruiterProfile`. Domains match exactly, so `@corp.gmail.com` and `@notgmail.com` stay valid company domains. New suite `npm run test:work-email` (15 checks) mixes pure rule tests with source assertions that every writer enforces the rule, that each guard runs before the write it protects, and that no `ALLOW_PERSONAL`/`SKIP_WORK_EMAIL` style flag exists. No schema change, no migration, no feature flag. New read-only script `npm run db:audit:personal-domain-recruiters` (`prisma/scripts/audit-personal-domain-recruiters.ts`) counts and lists the `RecruiterProfile`, `VerifiedRecruiterSeat` and stray `RecruiterEmailOtp` rows on a personal domain; it filters in JS through `isPersonalEmailDomain` rather than a SQL `endsWith` chain, so the list it audits against cannot drift from the one the product enforces. OPEN: any `RecruiterProfile` created before this change on a personal domain still exists and can still sign in — approving it is now refused, but sign-in is deliberately left unguarded so a pre-T-225 account is not locked out silently. The audit reports zero rows on the dev database; PRODUCTION HAS NOT BEEN AUDITED.
- 2026-09-09 [feature|ui] Registered hackathon participants can now see their team code and roster: a new read-only `TeamPanel` renders on `/hackathon` for `TEAM` entries (fed by the `getMyRegistration` result the page already fetched), `/hackathon/dashboard`'s coming-soon branch now renders `TeamRoster` + `InvitePanel` so the four surfaces that route there aren't dead ends, `SuccessPanel` shows the team code to `TEAM_JOIN` registrants (not just `TEAM_CREATE`), `InvitePanel` stays visible once a team is full, and the registration dialog refreshes on close. The landing panel is gated server-side on `entryType === "TEAM"` so a SOLO entrant's team code is never serialized into the RSC payload. Plan 116; `SHOW_LIVE_DASHBOARD` untouched.
- 2026-09-10 [schema|rule|env] Plan 120: Profile Performance counters on /profile — Search appearances (DETAIL_VIEW) and Recruiter actions (RESUME_UNLOCK) over a rolling 90 days from new `CandidateProfileEvent` (migration 20260910180000_candidate_profile_event, additive, NOT YET APPLIED). Guests dedupe via daily-rotating `HIRE_VIEW_SALT`+IST+IP+UA hash (no tracking cookie, no raw IP stored). Resume unlock seam is `canViewResume()` → CONTACT_SHARED today; no billing built.
- 2026-09-10 [schema|rule] Plan 117: profile-derived discoverability via new TalentCandidateSource.PROFILE track (usable profile = searchableUserWhere + ≥1 claimed skill + non-blank fullName; no openToWork / evidence gate) plus opportunity-type ANY-overlap hard filter and TalentEmploymentType.FREELANCE on the recruiter side (migration 20260910130000_talent_profile_source_and_freelance, additive, NOT YET APPLIED).
- 2026-09-10 [schema|rule|convention] /profile fixes across Basic Information, dates, Skills and Accomplishments. SCHEMA: new `CandidateGender` enum (MALE/FEMALE/TRANSGENDER) + nullable `CandidateProfile.gender` (migration 20260910120000_candidate_gender, additive, NOT YET APPLIED); NULL means not stated, so nothing is backfilled. Country Code is gone from the editor — the candidate now picks a country NAME from `src/lib/country-catalog.ts` and it still stores ISO-3166-1 alpha-2 in the existing `countryCode`, so registration, the OTP dialing-code split and /hire are untouched and nothing is migrated. MANDATORY FIELDS: only what /register already collected stays required — Full name in Basic Information, and phone when OTP is on. Every repeatable section is optional; entirely blank rows are dropped at the boundary (`isBlankRow`), and a row that HAS been started still needs its anchor (`requireAnchor`: employer + start year for Experience because `CandidateExperience.startedOn` is NOT NULL, institution for Education, name for Certification, title for Project). COMPLETENESS is no longer all-or-nothing: each section reports a `fraction` of its own field checklist and earns that share of its weight, so a half-filled section moves the number; `complete` still means all of it and the 125-point spread still caps at 100. VALIDATION: Full name / City / State reject digits and symbols (Unicode letters plus . ' - & so "D'Souza" and "Jammu & Kashmir" pass); Education years are bounded 2000..current with the end-year dropdown starting at the start year and running at most EDUCATION_MAX_SPAN_YEARS (5) past it; Experience end-year options start at the chosen start year; certification issue dates cannot be in the future (year list stops at this year, month list at this month) and a "This certificate does not expire" checkbox collapses the expiry pair — UI-only, it saves as a null expiry, no column added. Certifications capped at MAX_CERTIFICATIONS = 10. `firstIssue` no longer prints raw field names: a message that already reads as a sentence is shown as-is, otherwise the field is humanised ("expiresYear" → "Expires year"). SKILLS: new curated `src/lib/skill-catalog.ts` drives both the Skills picker and the Project tech-stack tags, replacing the `PROFILE_QUICK_SKILLS` list in candidate-vocab (deleted) — the `Skill` table was seeded from free-text StudentProfile.skills, which is where the junk names and the Tailwind css / Tailwind CSS / TailwindCSS triplets came from. `canonicalSkillName` folds aliases before any claim is made, API results are folded and de-duplicated in the combobox, and `prisma/scripts/dedupe-skills.ts` (npm run db:dedupe:skills, DRY RUN unless --apply) merges the existing duplicate rows, moving CandidateSkill/SkillEvidence/ActivitySkill/ProgramSkill/JobSkill/AssessmentScore onto the keeper. Fixed the bug where Enter in the skill search submitted the section — it reported "Skills saved" for a skill that was never added; Enter now picks the first match or adds the typed text, and PwTags stops propagation too. Certification name suggests well-known Coursera/Udemy/NPTEL/vendor courses and AUTOFILLS the issuer; issuer suggests NPTEL/Udemy/Coursera/Udacity/CBSE etc. and still takes free text (`src/lib/certification-catalog.ts`). LAYOUT: DashboardFooter moved INSIDE the shell's scroll container (it sat below a viewport-height column, which is why it never scrolled into view on /dashboard or /profile); the unsaved-changes prompt is now an inline popover anchored above Save/Cancel instead of a centred modal, which also removes the z-index fight with the sheet; the sheet freezes `.abt-content-scroll` as well as body; the profile page's scrollbar is hidden via a `body.pw-profile-page` class the wizard sets, so no other route loses its scrollbar; About is a 4-row textarea instead of one that grew to fill the sheet.
- 2026-09-09 [schema|rule] T-226: one independent workspace per recruiter. `provisionRecruiterIdentity` slugged its `Organization` on the COMPANY NAME on purpose, so colleagues shared one org and could share talent lists; the September product contract (Demo88 §2) says every recruiter works alone and multi-user company hiring is Phase 2 / Contact Sales, so `orgSlug(company)` is now `recruiterWorkspaceSlug(userId, company)` — readable company half plus the last 8 chars of the recruiter's user id. `Organization`/`OrganizationMember`/`UserRoleAssignment`/`TalentList` are UNCHANGED in shape: the table stays because `TalentList.organizationId` is a required FK and 078 Phase 7 is frozen for September; what changed is that a row now means one workspace belonging to one person. This was cheap because the shared-company model was write-only — `provision-recruiter.ts` was the only file in `src/` touching those three models and `TalentList` had zero readers. Migration `20260909180000_recruiter_independent_workspace` adds `RecruiterProfile.setupStep` (new enum `RecruiterSetupStep` PROFILE/COMPANY/COMPLETE, default PROFILE) + `setupCompletedAt`, and BACKFILLS every existing row to COMPLETE so no current recruiter is thrown into a wizard. `RecruiterState` gains `setup_incomplete`, decided BEFORE `pending` — an unfinished application is not "under review". New `/talent/setup` (server) + `recruiter-setup-form.tsx` (client, the only one) resume from the stored step; `COMPLETE` with a null `setupCompletedAt` resumes at COMPANY rather than showing a false success. `completeRecruiterSetupAction` provisions the workspace inside the same transaction that marks setup complete, so an abandoned wizard leaves no empty Organization. New `features/recruiter-workspace/workspace.ts` exports `requireRecruiterWorkspace()` — takes NO parameters and resolves the caller from `auth()`, closing the "recruiter A passes recruiter B's id" class by signature; this is the boundary T-255 will build the isolation sweep on. Four redirecting surfaces route `setup_incomplete` to /talent/setup (talent login/register/pending, `/`); the surfaces that only compute `approved`/`pending` booleans are correct unchanged. Approval is NOT removed — setup now precedes it. T-225 untouched (15/15 still green). New suite `npm run test:recruiter-workspace` (13 checks). Covers TC-R-003.
- 2026-09-11 [schema|ui|rule] T-227: recruiter profile and company identity. Recruiter can view and edit their profile (fullName, phone) and company identity (companyName, website, industry, sizeBucket, location) at /hire/settings. Added nullable location column to Organization (migration 20260911230000_organization_location, additive, zero-downtime). Created getRecruiterProfileAction and updateRecruiterProfileAction in src/app/actions/recruiter-profile-actions.ts scoped strictly by session workspace via requireRecruiterWorkspace(), synchronizing RecruiterProfile.company with Organization.name in an atomic transaction. Added /hire/settings page and RecruiterProfileForm with validation and feedback. Added Settings link to HireSidebar for authenticated recruiters. Automated test suite in src/features/hire/recruiter-profile.test.ts (15 passing).
- 2026-09-14 [schema|rule|convention] Snowflake Data & AI Engineering cohort (plan 141) ships 078-native at /program/snowflake, a fourth copy of the Databricks/DS-Architect/Power BI open-cohort shape (deliberately NOT abstracted): LearningProgram "snowflake" + ROLLING open Cohort "snowflake-open", plain-cuid ProgramEnrollment/ActivityAttempt/ActivityEvaluation/EnrollmentProgress/EnrollmentDayActivity, no dual-write, no legacy ProgramMember/ProgramDay row. Content was AUTHORED from the 15-Day Snowflake training-program doc into prisma/content/snowflake-cohort (modules + days.missionSpec + videos seeded; concept-questions/rubrics stored but unread); activity ids act_snf_day_NN / act_snf_vid_NN_NN. Day 1 = enrolment date in IST, 15 days, 6 modules, 245 pts (Day 1 DATA_ROOM 10, days 2-13 SHIP_IT 15, days 14-15 project days 25+30), 30 runs/day, 15s spacing. Unlike powerbi-cohort, videos.json ships real youtubeIds (29, all live on 2026-09-14). repoChecks content regexes are JS-valid (no inline (?i) flags). Hub heatmap counts act_snf_day_* attempts; Prep Kit gains a fifth card that wraps at xl; curriculum-skills.json gains a 12-skill "snowflake" entry. Gated by ENABLE_SNOWFLAKE. Seeded on the ep-young-shadow-amawetjy dev branch only; production NOT seeded.
- 2026-09-22 [schema|rule|convention] 15-day Databricks Data & AI Engineering cohort ships 078-native at /program/databricks-ai, a fifth copy of the open-cohort shape (cloned from Snowflake, deliberately NOT abstracted) and a SEPARATE track from the 31-day /program/databricks cohort: LearningProgram + ProgramCategory "databricks-ai" + ROLLING open Cohort "databricks-ai-open", plain-cuid ProgramEnrollment/ActivityAttempt/ActivityEvaluation/EnrollmentProgress/EnrollmentDayActivity, no dual-write, no legacy ProgramMember/ProgramDay row. Content AUTHORED from the 15-Day Databricks Data & AI Engineer Training Program doc into prisma/content/databricks-ai-cohort (modules + days.missionSpec + 28 live videos seeded; concept-questions/rubrics stored but unread); activity ids act_dbai_day_NN / act_dbai_vid_NN_NN (prefix does not collide with act_dbx_day_). 15 days, 6 modules, 245 pts (Day 1 DATA_ROOM 10, days 2-13 SHIP_IT 15, days 14-15 project days 25+30), 30 runs/day, 15s spacing, Day 1 = enrolment date in IST. Notebook repoChecks expect Source-format .py files (Day 1 tells learners to set it). Hub heatmap counts act_dbai_day_* attempts; Prep Kit gains a sixth card "15 Days Databricks Data & AI" plus a Prep Kit search entry; the hero reuses public/databricks-cohort/databricks-hero.svg. Gated by new env var ENABLE_DATABRICKS_AI. prisma/seed-databricks-ai.ts refuses BOTH ep-young-shadow-amawetjy and ep-nameless-term-ams9a5e3 unless SEED_ALLOW_PRODUCTION=true. NOT seeded anywhere yet; curriculum-skills.json and career-guidance NOT updated (cross-module, pending owner approval).
